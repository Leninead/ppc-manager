# 2026-06-03 — M30 Pricing Dashboard: F3.2 (parsers + lookups) CERRADA

> Frente único del día. Worktree feature/m30-pricing-port. Continuación de F3.1 (2026-06-02).

## Output — 4 commits (branch feature/m30-pricing-port, pusheado al cierre)
- `cfaa83d` feat(M30): parsers 6 fuentes + lookups
- `6b173b8` fix(M30): izzi sheet selection + cogs hint + csv sep autodetect
- `a89fc4b` chore(tests): testpaths=tests + revert path-hardening (neutraliza landmine scratch)
- `86afa4d` test(M30): blindar bug separador + divergencia parseCSV (MAYOR code-reviewer)

HEAD: 86afa4d.

## B3 — 6 parsers (@st.cache_data, puros, bytes→DataFrame)
fba/fee/awd (CSV, sep ;/, autodetect + utf-8-sig) · pl/maestro/izzi (XLSX). Columnas verbatim.

## B4 — 3 lookups (planos, toman DataFrame, NO cacheados)
_build_cogs_lookup (last-valid-wins + __month__* strings) · _build_fee_lookup (agrega/promedia/None) ·
_build_maestro_lookup (key lower + fila completa, last-wins). Clave SKU siempre str().

## 3 desviaciones corregidas (6b173b8)
1. **izzi (bug real)**: leía primera hoja; el HTML lee por nombre 'Inventario 2526' con fallback (||).
   Fix: sheet_name explícito + header=None (espeja sheet_to_json {header:1}). Sin el fix arrastraba
   la hoja equivocada a F3.3 en silencio.
2. cogs hint: dict[str,float] → dict[str,float|str] (el verbatim mete __month__* strings). Runtime-neutral.
3. csv sep: replicado autodetect ;/, del HTML L666-669 (utf-8-sig ya estaba de F3.2).

## code-reviewer Opus 4.7 read-only
APROBABLE para F3.3 · 0 bloqueantes · 2 MAYOR · 8 menor + 6 nit (cosméticos). Categorías core LIMPIAS
(pureza parsers, @st.cache_data solo en bytes/no en builders, 3 lookups verbatim, izzi sheet+header,
type hints, characterization tests).

## 2 MAYOR blindados (86afa4d)
- M1: test congela el bug del separador crudo dentro de comillas (cuenta ;/, sin quote-awareness, verbatim).
- M2: docstrings corregidos (se elimina la afirmación "1:1") + test congela el comportamiento ACTUAL de
  pandas (no trimea, skip_blank_lines, fila de 1 campo → NaN). La decisión de portear el parseCSV custom
  se DIFIERE a F3.3.

## Learnings
### Landmine de colección pytest (RESUELTO en branch m30)
`scripts/_scratch_M27/test_b5b_extract.py` (tracked) y `scripts/smoke_b6a_e2e_pipeline.py` hacen
`sys.path.insert(0, <MAIN>)` al ser recolectados por `pytest` bare → contaminan `sys.modules['modules.pages']`
hacia MAIN → rompen el import de módulos worktree-only. Fix de raíz: `pytest.ini` con `testpaths = tests`
(creado en root del worktree). Neutraliza TODOS los scripts, no solo el scratch.
**OJO**: vive en feature/m30-pricing-port, NO en main → propaga al mergear M30, o aplicar a main aparte si se quiere antes.
### Deuda pre-existente confirmada (ajena a F3.2)
4 failed / 2 errors en tests/ (b7_importer TestCopyOverridesPreserved ×2, m29_ui_e2e ×3 FileNotFoundError,
datadive_to_v3_mapper merge): fixture-dependientes (proposal v12 gitignored + secrets.toml ausentes en
worktree). Es la P2 ya documentada. Fix real: fixtures trackeados en tests/fixtures/. No bloquea M30.
### Entorno worktree m30
Sin .venv propio. Tests con el venv 3.12.10 de MAIN prestado (pandas 2.2.3). F3.4 (streamlit run) va a
necesitar .venv en el worktree o correr explícito contra el de MAIN.

## Pendiente F3.3
- Scoring: _compute_ais + _compute_score (20+ reglas) + _enrich_record + _run_analysis (orquestador).
- Bug 30-vs-37: replicar VERBATIM (L837=37, L1029=30; dead-code por guard !restock_alert). NO arreglar.
- DECISIÓN consciente: ¿portear parseCSV custom (trim por celda + descarte <2 campos)? El trim de columnas
  string impacta las comparaciones verbatim de strings Amazon ('Excess','Low stock'...). Decidir con el
  scoring delante. Si se portea → actualizar TestCsvDivergenciasHTML conscientemente.
- AIS = Aged Inventory Surcharge (8 buckets), no "Amazon Inventory Score".

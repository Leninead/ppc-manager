---
fecha: 2026-05-09
duration_estimated: 4h
duration_actual: ~4h focalizado
sesion_anterior: 2026-05-08
commits_del_dia:
  - 841b230
  - 0c7dbf4
---

# Daily 2026-05-09 — Validacion E2E de M28 + audit code-reviewer (3 bugs cerrados)

## TL;DR

- 5/5 features funcionales de M28 SKU Progress Report pasaron smoke test con CSV real de Dermaglos US (B0CYLMJJJC, W19, 10 ASINs).
- 4 bugs cerrados en el dia: 1 en sesion (commit 841b230) + 3 detectados por audit del code-reviewer (commit 0c7dbf4).
- Primera invocacion operacional real del code-reviewer Opus 4.7 en M28 = PASS. Encontro bug C2 (envenenamiento ASIN vacio) que el flujo humano no detecto.
- Validacion triple de los fixes: estatica (audit) + visual (diff manual) + funcional (E2E con SKU artificial sin ASIN). M28 queda production-ready local.
- Persistencia compartida en cloud sigue bloqueada esperando respuesta de Freddy.

## Bloque 1 — Validacion features 1-3 con datos reales (~1.5h)

Smoke test E2E con CSV real bajado de la cuenta Seller Central US de Dermaglos:

- Archivo: `BusinessReport-5-08-26__2_.csv` (semana W19, May 4-10)
- 10 ASINs en el reporte, ASIN target: B0CYLMJJJC (Moisturizing Cream)
- Metricas del ASIN target: 1,935 sessions / 167 units / $1,668.33 sales / CVR 8.63%
- Mapping critico descubierto: PVENUS0782 (SKU interno) ↔ B0CYLMJJJC (ASIN), 1:1 sin variantes

Features cubiertos en este bloque:

1. ✅ Crear cliente + agregar SKU (re-creacion tras corregir slug `dermaglos`)
2. ⏳ Registrar optimizacion (validado en bloque 2)
3. 🐛 Importar CSV — bug encontrado y fixeado en sesion (ver bug 1)

**Bug 1 fixeado durante el bloque (commit 841b230):** `_consolidate_rows_by_sku` construia el dict de matching con `s["sku"]` como clave en lugar de `s["asin"]`. El porter heredo del HTML legacy de Marcos la asuncion implicita SKU=ASIN. En el modelo nuevo M28 el SKU es humano (PVENUS0782) y el ASIN es metadata separada (B0CYLMJJJC). El CSV de Amazon trae el (Child) ASIN en la columna `_id`, entonces el matching contra SKUs trackeados daba siempre 0. Fix de 1 token: `s["sku"]` → `s["asin"]`.

Validacion post-fix: el preview del importador paso de 0/10 a 1/10 SKUs reconocidos. Snapshot 2026-W19.parquet escrito en `data/account-health/dermaglos/sku-progress/` (10.5 KB). `_history.parquet` agregador creado (11 KB). KPIs y charts Plotly renderizando con la data real.

## Bloque 2 — Validacion features 4-5 (~30 min)

**Feature 2 — Registrar optimizacion:** form simple con 2 campos (semana de aplicacion en dropdown auto-llenada con periods existentes + descripcion libre). Submit creo `optimizations.parquet` (4.2 KB) con schema correcto: `sku/week_iso/year/label/timestamp`. Visible en la pildora "Optimizaciones registradas" del tab del SKU.

**Feature 4 — Borrar snapshot puntual:** boton 🗑️ del lado izquierdo en tab Admin (cuidado: el del lado derecho borra el SKU del config). Click ejecuto `target.unlink()` correctamente. `_history.parquet` tambien borrado por la rama de fallback del except. `optimizations.parquet` y `tracked-skus.json` quedaron intactos — separacion correcta entre log y snapshot.

**Feature 5 — Excel completo:** archivo nombrado `dermaglos_SKU-Progress_2026-W19.xlsx`, 4 hojas: Resumen (5×9), Detalle (1×16), Optimizaciones (1×5), PVENUS0782 (1×9). Data cross-source consistente: UI = Parquet = Excel.

**Resultado del bloque:** 5/5 features funcionales pasaron smoke test inicial.

## Bloque 3 — Bug intermitente del Excel post delete + reimport (~20 min)

Despues de borrar W19 + reimportar el CSV + bajar el Excel, las hojas Resumen, Detalle y PVENUS0782 salieron contaminadas con filas fantasma de `_period="optimizations"` y campos numericos en NaN. La contaminacion es dramatica: pandas hace outer join al concatenar Parquets con columnas distintas, entonces las filas del log de optimizaciones aportan columnas `label/timestamp` (con NaN en el resto) y las filas del snapshot semanal aportan columnas de KPIs (con NaN en label/timestamp). Y como las metricas son additivas, la suma de cualquier valor con NaN da NaN.

Diagnostico inicial: probable bug en `_rebuild_history` que concatena `optimizations.parquet` junto con snapshots semanales. Pero al re-testear el flujo el bug NO se reprodujo (segundo Excel salio limpio). Catalogado como "bug intermitente sospechado, no reproducible bajo demanda".

Decision senior: parar de intentar reproducir empiricamente e invocar code-reviewer para analisis estatico. Mucho mas eficiente que F5 + restart loop.

## Bloque 4 — Audit del code-reviewer Opus 4.7 (~30 min)

Primera invocacion operacional real del code-reviewer despues del model fix de la sesion 2026-05-06 (cuando 7 agentes pasaron de Sonnet 4.5 deprecated a Opus 4.7 / Sonnet 4.5 snapshot fijo).

Output: 1 reporte estructurado con 4 findings priorizados:

- **C1 (CRITICAL):** `_list_periods` en core/persistence.py matchea `optimizations.parquet` como si fuera snapshot semanal. Confirma analiticamente la hipotesis del bloque 3.
- **C2 (CRITICAL):** `_consolidate_rows_by_sku` tiene vulnerabilidad de envenenamiento por ASIN vacio + substring poison. NO detectado humanamente en el smoke test ni en el diff visual del fix 841b230.
- **I1 (HIGH):** cache stale tras `target.unlink()` en `_render_admin_tab`. Explica la intermitencia del bug del Excel — el cache decide si C1 dispara o no.
- **I2 (MEDIUM):** descartado por dependencia con C1 (al fixear C1, I2 deja de importar).

Lecciones operacionales del audit:

- code-reviewer encontro C2 que humanos podrian pasar de largo (es un caso edge: SKU sin ASIN cargado). Bug latente, hubiera explotado en uso real cuando un AM agregara un SKU sin completar el campo ASIN opcional.
- Validacion operacional del agente = PASS. Output estructurado, accionable, sin falsos positivos.

## Bloque 5 — Aplicar 3 fixes via Claude Code con super-prompt (~25 min)

Invocacion de Claude Code con super-prompt detallado conteniendo: guard de repo + 3 fixes con old_string/new_string exactos + 7 validaciones + criterios de exito + reglas de no-commit-sin-aprobacion.

Working tree no estaba clean al primer intento por residuo del bug fantasma del sop-writer en `notes/daily/2026-04-24.md` (cambio fantasma de line endings LF→CRLF que git auto-renormalize muestra cada vez que algo lee el archivo). Resuelto con `git checkout notes/daily/2026-04-24.md`. Working tree clean al segundo intento.

3 fixes aplicados en orden: C1 (core/persistence.py) + I1 + C2 (sku_progress_report.py).

**Decision senior del agente:** indentacion del fix I1 a 24 espacios en lugar de 20 que pedia el spec. La razon: 20 sp habria sacado `st.success` y `st.rerun` fuera del `if target.exists():`, disparandose incluso cuando el archivo no existia. A 24 sp las 4 lineas (comentario + 2 clears + st.success + st.rerun) quedan al mismo indent dentro del guard, preservando el principio del usuario ("invalidar SIEMPRE que se borro el target") sin abrir flancos nuevos. Override aceptado y documentado.

Diff stats: 2 archivos, 21 inserciones, 2 deleciones. Commit 0c7dbf4 + push a origin/main exitoso.

## Bloque 6 — Validacion E2E de los 3 fixes con datos reales (~20 min)

**Test C1 + I1:** F5 despues del commit mostro KPIs en NaN. Hipotesis: el `_history.parquet` generado pre-fix sigue contaminado en disco (los fixes en core/persistence.py NO auto-curan archivos derivados ya escritos). Validado con `pd.read_parquet`: 2 filas / 18 columnas / `periods=['2026-W19','optimizations']`. Solucion: `rm _history.parquet` + F5 → modulo regenero archivo limpio. Re-validacion: 1 fila / 16 columnas / `periods=['2026-W19']`. KPIs reales en UI. PASS.

**Test C2 (catastrofico):** agregar un SKU artificial sin ASIN (`TEST_SKU_SIN_ASIN_2026-05-09` con `asin=""`). Importar el mismo CSV. Esperable post-fix: 1 reconocido (PVENUS0782) / 9 descartados. Esperable pre-fix: 10 reconocidos / 0 descartados (envenenamiento total). Resultado real: 1/9. PASS.

Cleanup post-test: TEST_SKU borrado del cliente. Cliente queda con solo PVENUS0782 + 1 snapshot W19 + 1 optimizacion del feature 2.

## Smoke tests — resumen final

- 5/5 features M28: PASS
- 3/3 fixes audit code-reviewer: PASS validados con data real

## Bugs encontrados y fixeados (4 totales)

1. **Matching SKU=ASIN** (commit 841b230, sev CRITICAL): asuncion heredada del HTML legacy de Marcos, no compatible con el modelo nuevo M28 que separa SKU humano + ASIN. Fix: 1 token (`s["sku"]` → `s["asin"]`).

2. **C1 — `_list_periods` glob laxo** (commit 0c7dbf4, sev CRITICAL): `glob('*.parquet')` matcheaba `optimizations.parquet` como snapshot semanal. El filtro `name.startswith("_")` solo capturaba el `_history.parquet` agregador, no los logs append-only del Agency OS. Fix: lista negra `("optimizations", "events", "decisions-log")` en addition al filtro existente.

3. **C2 — envenenamiento ASIN vacio** (commit 0c7dbf4, sev CRITICAL): el fix 841b230 cerro un bug pero abrio un flanco nuevo. Si un SKU tiene `asin=""`, el dict tiene clave `""`, y el substring fallback consolida TODOS los ASINs del CSV bajo ese SKU porque `"" in cualquier_string == True`. NO detectado en smoke test humano ni en diff visual del fix anterior — encontrado por code-reviewer Opus 4.7. Fix: filtro `if s.get("asin", "").strip()` en el comprehension + defense-in-depth con `MIN_MATCH_LEN=6` en el fallback.

4. **I1 — cache stale post-unlink** (commit 0c7dbf4, sev HIGH): el patron de invalidacion (`_list_periods.clear()` + `_load_history.clear()`) que existia en `_delete_cliente` no se replicaba en `_render_admin_tab` cuando se borra snapshot puntual. Causaba el bug intermitente del Excel — segun el estado del cache, C1 disparaba o no. Fix: agregar las 2 invalidaciones de cache despues del `target.unlink()`.

## Lecciones aprendidas

1. **`Get-Content -Raw` rompe Unicode en PowerShell 5**: leer archivos con emojis usando `Get-Content -Raw` los corrompe (`📦` → `Ã°Å¸â€œÂ¦`). Workaround: `[System.IO.File]::ReadAllText($file, [System.Text.UTF8Encoding]::new($false))` tanto en lectura como en escritura. La documentacion previa solo cubria escritura (Set-Content); ahora sabemos que lectura tiene el mismo problema. Agregar al CLAUDE.md raiz en seccion "Gotchas de PowerShell" en invocacion 3.

2. **Nunca crear scripts en raiz del repo con nombres de modulos stdlib**: `inspect.py` provoca circular import en pandas (numpy importa `inspect` internamente y agarra el cwd primero). Lista de evitar: `inspect.py`, `random.py`, `math.py`, `email.py`, `json.py`, `csv.py`, `time.py`, `os.py`, `sys.py`, `io.py`, `string.py`, `re.py`, `socket.py`, `logging.py`, `tokenize.py`, `pdb.py`, `tempfile.py`. Usar prefijo `_` o sufijo descriptivo (ej: `_check_history.py`).

3. **Fixes en core/persistence.py NO auto-curan archivos derivados ya generados**: `_history.parquet` queda contaminado hasta que se regenere desde cero. Regla preventiva: despues de cualquier cambio en core/persistence.py que toque generacion de agregadores, sweep manual con `Get-ChildItem -Path data -Recurse -Filter "_history.parquet" | Remove-Item`. Critico para multi-cliente — un solo history contaminado envenena todo el dashboard de ese cliente.

4. **Validacion operacional del code-reviewer Opus 4.7 = PASS**: primera invocacion real encontro C2 que el flujo humano (smoke test + diff visual del fix 841b230) no detecto. Doble valor de la sesion: modulo mas robusto + agente validado para futuros audits sin necesidad de double-check humano.

5. **Senior judgment del agente sobre prompt literal**: code-reviewer aplico indent 24 espacios en fix I1 cuando el spec pedia 20, porque 20 habria sacado `st.success`/`st.rerun` fuera del `if target.exists():`. Override correcto. Leccion para mi: validar indentacion contra archivo real antes de fijar numeros absolutos en prompts; preferir "alinea con lineas X e Y" que "20 espacios literal".

## Deuda tecnica anotada (no fixear)

- Form "Agregar SKU" hereda titulo del SKU previo si el campo queda vacio (deuda menor UX, descubierto al re-crear cliente dermaglos).
- Anotacion truncada `test smoke 2026-05-0` en chart Evolucion del SKU (label de optimizacion usado como anotacion sin truncate inteligente).
- `st.rerun()` post-success demasiado rapido para leer toasts (UX friction, ya documentado en sesion 2026-05-08).
- Bug heredado "substring bidireccional" en `_consolidate_rows_by_sku` sigue presente con length guards (MIN_MATCH_LEN=6) pero sin solucion definitiva. Aceptable para v1.
- Placeholders del form Agregar SKU se ven como valores cargados (gris vs vacio real). UX confuso en primer uso.
- Nombre de hoja Excel `PVENUS0782` puede romper con SKUs >31 chars (limite Excel). YAGNI hoy, anotado para cuando aparezca primer caso.
- Bug recurrente git renormalize en `notes/daily/2026-04-24.md`: el archivo
  aparece como modificado cada vez que git toca el repo (3 veces hoy: al
  inicio de sesion, durante aplicacion de fixes via super-prompt, y al
  cierre de sesion para escribir este daily). Workaround actual: `git
  checkout notes/daily/2026-04-24.md`. Causa probable: inconsistencia entre
  line endings en disco (CRLF) y regla de normalizacion de git (LF segun
  core.autocrlf o .gitattributes). Fix definitivo pendiente para sesion
  separada: `git add --renormalize` + commit del archivo, o regla explicita
  en .gitattributes con `*.md text eol=lf`.

## Validacion operacional de agentes

- **code-reviewer (Opus 4.7):** ✅ PASS primera invocacion operacional. Encontro bug C2 que humanos no detectaron. Senior judgment correcto sobre prompt literal (indent fix I1). Cero falsos positivos en findings. Output estructurado y accionable.

Pendientes de validar (de model fix sesion 2026-05-06):

- ppc-module-builder, atom11-specialist, excel-export-builder, testing-agent, client-onboarding (5 agentes sin invocacion operacional todavia).

## Cierre — pendientes para proxima sesion

**Tooling:**

- Pasada UX/UI a M28 con agente ui-designer (lista de 5+ deudas anotadas arriba).
- Migracion data Gamboa legacy del HTML a Parquet (15 semanas × 21 SKUs, script one-shot ~2-3 hs).
- Badge "modulos activos" del Inicio Agency OS marca 23, esperable 27 (deuda del router, sesion separada chica).
- Variation Builder M26 fix (3 keys data_editor + testing E2E).

**Operativos cliente:**

- LTD: respuesta Adam B005ULUZIQ + Agustin restock B09MG1J3LC + asignacion portfolios 5 EXACT.
- Dermaglos: Rufus 4 heroes + negativizaciones 18 terminos + harvest 7 KWs.
- Setex: portfolios 9 nuevas + reposicion Tati + ETA respuesta.

**Decision arquitectonica:**

- Persistencia compartida en cloud (Supabase / R2 / Railway / Neon) pendiente de respuesta de Freddy. Bloqueante para uso real con equipo + porting M29 Pricing Dashboard.

**Estado vault al cierre:**

- 27 modulos activos en 6 secciones.
- M28 production-ready local con 4 bugs cerrados, validado triple (audit + visual + funcional).
- Account Health seccion con 2 modulos: M27 Flat File Migrator (stateless) + M28 SKU Progress Report (con persistencia).
- M29 Pricing Dashboard sin tocar (esperando decision persistencia).

Estado de salud del proyecto: ✅ verde

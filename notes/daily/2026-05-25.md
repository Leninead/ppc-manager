---
fecha: 2026-05-25
tipo: daily
clientes_tocados: [setex]
features_tocados: []
---

# Daily 2026-05-25

## Setex

**Foco:** P0 Hot Sale — push táctico a heroes Ultra Thin / 1mm / Thick alineado con pedido Tati 10:39. Análisis 360° (STR 30d + SQP semana 21 + cruce Inventory + Campaign CSV + BSE) → 6 bulks ejecutados 100% Success.

### KPIs cuenta pre-bulks (BSE 1-25/05)

- ACoS 18.5% (mejoró 0.9 pts desde 19.4% pre-22/05) — pisando target cuenta 18%
- ROAS 5.41× (+0.26× vs 5.15×)
- 448 orders / 30d (+14% vs 393)
- CVR 11.45% (+1 pt vs 10.46%)
- Cuenta arranca Hot Sale en buen estado, no rescatando

### 6 bulks ejecutados — UUIDs Amazon Ads Console

- Bulk 0 PausarB09F7P8GBZPA (17 rows): `35acaeab-06fd-4453-bfed-0aa53f442947`
- Bulk 1 PushHeroesROAS10x (24 rows = 12 camps + 12 TOS adj): `8649da59-f84a-4cc7-a1a6-516110e5745c`
- Bulk 2 BidUpKWHeroes (11 rows): `a3d34f10-91c7-4745-8ca6-22934dfd946e`
- Bulk 3 PausasWaste (8 rows): UUID parcial `?2a0-d940-48dc-8099-84126a0dfda1`
- Bulk 4 ReactivarB086H3TZ6B (19 rows): `6e51010d-c5da-4cbc-8772-0c3423da9466`
- Bulk 5 NegativosQuirurgicos (153 rows): `8c097629-806a-4237-a79b-96bf669074a4`

Net impacto budget: +$430/d push winners / -$421/d waste pausadas = ~+$9/d neto con redistribución masiva hacia ROAS 10-28× con stock validado. Cuenta pasa de 86 → 78 camps ENABLED.

### Decisiones cerradas

1. **B09F7P8GBZ stock 6u — Opción B (pausar 17/18 PAs)**: preservar 1 PA del ad group `AG | almohadillas para lentes` ($1,170/4 ord/ROAS 7.7×). Trip wire activado: si OnHand <3u sin inbound confirmado → pausar el último también. Monitor stock semanal cada lunes.
2. **Listing audit EN B081GB8F89 — bajar prioridad**: diagnóstico previo parcial incorrecto. SQP nueva muestra cluster EN convierte bien (`nose pads for glasses` PS 66.7%, `nose pads` PS 100%, `anti slip glasses nose pads` PS 100%). Funnel break es específico de 2 subgrupos (queries "grip" puro + rayban-specific) → ya negativizados en Bulk 5.
3. **Listing audit ES B081GB8F89 — refuerzo evidencia, sigue prioritario**: queries con specs técnicas exactas (`almohadillas para lentes nariz antideslizantes 1 mm`, `gomas para lentes antiderrapantes`, `parches antiderrapantes para lentes`, `setex gecko grip 1mm`) muestran CS 100% pero PS 0% — comprador con specs sabe lo que busca, hace click, abandona en PDP. Listing no resuelve objeción premium.
4. **B086H3TZ6B recovery confirmado** (1u 12/05 → 49u hoy): reactivadas las 19 Product Ads pausadas en sesión 12/05.

### Bugs M4 nuevos detectados — consolidado para Ramiro

Suman a los 5 bugs previos (Dermaglos 08/05 + Setex 22/05). Total 8 bugs:

6. `ppc_insights_asin` corrupto/incompleto: solo 2 ASINs cuando hay $22k spend en decenas. Módulo filtra mal o join con BR roto.
7. `plan_accion_bulk` falsos positivos críticos:
   - `steam controller` clasificado como "⚡ ESCALAR" (gaming, IS 0%, ruido)
   - `nosepads setex for rayban meta wayferer gen 2` como "🛡️ DEFENDER marca" (es cross-brand rayban, va a negativos)
   - `setex nosepads` / `setex nose pad` / `setex pads` con BS 0%, pm 0 tratados como DEFENDER cuando son NaN en Brand Count del CSV (módulo trata NaN como 0)
8. `plan_accion_bulk` sin mapping de Campaign Name / Ad Group / Max Bid — bulks no ejecutables tal cual (mismo problema 3a sesión consecutiva)

### Gotchas y lecciones nuevas

- **Product Ad no tiene bid propio en Amazon SP**: hereda de KW/PT/AG. Pausar es la ÚNICA palanca por-SKU. Cuando se piensa "bajar bid del PA", en realidad las opciones son: bid KW (afecta todos los SKUs del AG), bid AG (idem), Daily Budget (mata camp entera), Bidding Strategy (no quirúrgico) o Negative Product Target con ASIN propio (= pausar). Patch para SOP `amazon-bulk-upload-guide.md` aprendizaje #17.
- **Cross-checking ASIN catálogo cliente al armar negativos**: M4 sugirió como negativos 4 self-ASINs Setex (B081GB8F89 badge, B08PZF22R1, B08C2T72ND, B0DK7Q4ZTY). Sin cruce contra catálogo cliente, los bulks M4 son inejecutables.
- **B09F7P8GBZ stock crítico no flageado previamente** en vault Setex — no estaba en lista de Tati pendientes 22/05. Detectado en cruce Inventory completo. Patch SOP: cruce Inventory completo obligatorio en sesión Setex (no solo lista pendiente cliente).

### Mensaje Slack Tati

Enviado con resumen Hot Sale push + flag URGENTE B09F7P8GBZ + corrección diagnóstico EN + pendientes cliente (B0DW9Z2H2W listing roto sigue, B0F63LTD92/B0C7WPFVGV restocked pero 0 sales 30d → flag listing audit nuevo, B0CC6THCDS precio $530 revisar).

### Próximas evaluaciones programadas

- 28/05 (día 3): impressions, CTR primer corte
- 01/06 (día 7): performance Hot Sale completa post-ventana
- 08/06 (día 14): review consolidada

### Pendientes para próxima sesión Setex

- [ ] Eval día 7 (01/06): impacto Hot Sale en cuenta + winners escalados + Brand Hub triplicado + 1mm AUTO COMPLEMENTS post +25%
- [ ] Confirmación Tati ETA inbound B09F7P8GBZ
- [ ] Status nuevos flags listing audit B0F63LTD92 / B0C7WPFVGV (restocked pero 0 sales 30d)
- [ ] Status sin novedad: B0DW9Z2H2W listing roto, B0CC6THCDS precio $530
- [ ] Status Atom11 v2026.3 confirmación Neha (sigue sin respuesta desde 22/05)
- [ ] Monitor stock B09F7P8GBZ cada lunes — trip wire 3u
- [ ] Bug list M4 consolidado (8 bugs) — entregable a Ramiro próxima sesión repo

Ver [[prompts/sesion/clientes/arranque-setex]] · [[brands/setex/setex]]

## M27 Flat File Migrator v1.1 — Sesión B6-b completa (8/8 sub-bloques)

**Chat #2/4 del día. Aislamiento estricto con chat M29 (#1) que tocaba proposal_studio.py en paralelo.**

### Commits del día (5)
- `f71bfdf` feat(M27): B6-b-1 orquestador `_run_migration_v11` cross-schema (~216 LOC + 91 LOC smoke)
- `7565d1d` feat(M27): B6-b-2 toggle radio v1/v1.1 con auto-deteccion de schema (+52 LOC)
- `95f936b` feat(M27): B6-b-3 renderer condicional v1/v1.1 + bloque diagnostics (+95 / -8 LOC)
- `b0a234c` fix(M27): restaurar B6-b-2 + B6-b-3 borrados accidentalmente por 64e3d6f (+147 / -8 LOC)
- `b9d9264` feat(M27): B6-b-4 expander 'Como funciona' dividido v1/v1.1 (+39 / -12 LOC)

### Trabajo del día — 4 bloques productivos + 1 fix de incidente

**Bloque 1 — B6-b-1 (`_run_migration_v11`, +216 LOC):**
Orquestador cross-schema v1.1. Shape output SUPERSET del `_run_migration` v1 (12 keys comunes + 5 nuevos: `enum_translators_active`, `diagnostics_b5b`, `diagnostics_b5c`, `diagnostics_by_code`, `coverage_pct`). 6 desvíos de spec documentados: keys reales `wb_bytes`/`sheet_name` (no `wb`/`ws`), `_locate_template_headers(wb)` devuelve dict 1-indexed, `_extract_old_rows` devuelve rows como dicts sparse, `_build_field_map` no expone método (síntesis re-derivada por `_normalize_label`), `skip_example` unconditional drop[0], smoke armado a mano para evitar `@st.cache_data` fuera de Streamlit. Smoke B6-b-1 PASS: rows=6, coverage=42.1%, label_match=66 + alias_label=3.

**Bloque 2 — B6-b-2 (UI auto-detect + toggle, +52 LOC):**
Bloque insertado en `_render_marketplace` entre uploaders parseados y botón migrar. `_open_ws` × 2 + `_detect_schema` × 2 → `recommended_mode` con 3 ramas. `st.radio` horizontal con `index` auto y caption descriptiva. Tratamiento de `"unknown"`/`""`/`None` como "no detectado" (corrección sobre spec, que asumía solo `None`).

**Bloque 3 — B6-b-3 (branch v1/v1.1 + diagnostics renderer, +95 / -8 LOC):**
Tres edits quirúrgicos: branch del orquestador (`if mode.startswith("Same-schema")`), edge cases (`no_field_ids` → `st.error` + sugerencia, `output_rows` vacío → `st.warning`), extras KPIs v1.1-only (Cobertura + Enum translators), sección "🔬 Diagnostics cross-schema (v1.1)" con tabla expander + 2 warnings (`unmapped_field`, `missing_required_in_new`) con preview de fields únicos. Nested helper `_preview_fields` introducido por DRY.

**Incidente — Chat M29 borró 147 LOC con `git add` no específico:**
Commit `64e3d6f` del chat #1 (M29 B7 UI dispatcher D2) tocó `flat_file_migrator.py` borrando 147 LOC de mis B6-b-2 + B6-b-3 ya commiteados. Causa raíz: VS Code abierto del lado de M29 con `flat_file_migrator.py` en versión stale (f71bfdf), `git add modules/` o `git add .` arrastró la versión stale al staging. Detectado durante smoke visual de Streamlit (botón "Migrar Data →" disabled, cards FLAT FILE / CATEGORY LISTING ausentes). Reflog mostró además que CC hizo `git commit --amend` sobre `64e3d6f` con mensaje fabricado "feat(M27): B6-b-4 rollback de renderer no usado" → SHA pasó a `252f286`. Resolución: `git restore --source=95f936b --staged --worktree modules/pages/flat_file_migrator.py` + commit forward `b0a234c`. `252f286` queda en historia como evidencia auditable del incidente.

**Bloque 4 — B6-b-4 (help expander dividido, +39 / -12 LOC):**
Expander "ℹ️ Cómo funciona" dividido internamente: sección "Modo Same-schema (v1)" (5 estrategias en cascada + cobertura 80-95%) + `st.divider()` + sección "Modo Cross-schema (v1.1)" (pipeline 4 pasos + cobertura 40-60%). CC corrigió 2 números de la spec: `_LABEL_ALIASES` real = 3 (no 40 como decía la spec), `_ALIASES` v1 = 61 (spec correcta, texto viejo del expander tenía "~40" desactualizado). Cambio de ejemplo enum ficticio "Cotton 100%" por uno real del código `_ENUM_VALUE_MAP`: "Partial Update → Edit (Partial Update)".

### Validación E2E (smoke visual Streamlit, par Gamboa real)
- ALRBB.xlsx (227 cols, 7 rows) → FLAT FILE detected
- coat.xlsx (294 cols, 3 rows) → CATEGORY LISTING detected
- Auto-detect: OLD=fptcustom → NEW=ptd → recomienda Cross-schema (default ON)
- Migración: 69 columnas migradas, 156 solo en nuevo, 95 no migradas, 6 filas
- Match por método: label_match: 66, alias_label: 3
- Cobertura: 42.1% | Enum translators: 3 | Example row saltada: 1
- Diagnostics: 276 events totales — 108 unmapped_field (18 fields únicos), 18 missing_required_in_new (3 fields únicos)
- Archivo descargado: coat_migrated.xlsx OK

### Decisiones cerradas
- Spec de mega-prompts tenía datos obsoletos sobre conteos de aliases (40 vs 3) — CC corrigió contra el código real. Lección: pre-validar contadores antes de prometer numeros en specs.
- Audit code-reviewer Opus (B6-b-5) NO ejecutado en esta sesión. M27 cierra al 100% funcional, validado E2E visual. Audit queda como deuda blanda P3.

### Deuda activa nueva (M27)
- **P1 NUEVA — bug entorno Python 3.14.3 + openpyxl 3.1.5 + .xlsm con macros:** smoke CLI crashea silencioso (exit -1, sin traceback, segfault nivel C) al abrir workbooks .xlsm. Streamlit UI también afectada (parseo silencioso / hang). Workaround validado: convertir `.xlsm` → `.xlsx` antes de upload (Excel: Save As). Solución definitiva: venv con Python 3.12.x.
- **P2 — síntesis `methods_count` v1.1:** re-derivada por `_normalize_label` con regla "hit directo = label_match, else alias_label". Validar contra comportamiento real de `_build_field_map` si se hace el audit Opus B6-b-5.
- **P3 — audit code-reviewer Opus 4.7 sobre delta B6-b completo:** queda pendiente. ROI bajo dado que E2E validado, pero patrón validado en sesiones previas (19/05, 20/05, 22/05) recomienda hacerlo antes de shipear.
- **P3 — artefacto untracked `-` en raíz** del repo: archivo accidental de un redirect mal formado (probable `> -`). No de M27. Limpiar con `Remove-Item -- -` o `git clean -f -- -` cuando se pueda.

### Lecciones operativas del día
- **Multi-chat paralelo en mismo repo:** "paths específicos siempre" no alcanza si IDE del otro chat tiene archivo stale abierto. Mitigación propuesta: lockfile manual `.locked-by-chat-N.txt` que cada chat verifica antes de `git add`, o IDE cerrado del lado que NO edita.
- **`git add modules/` es tan peligroso como `git add .`** cuando hay subarchivos modificados por otros chats. Solo `git add /.py` completo.
- **CC puede hacer `--amend` autónomo con mensajes fabricados.** Auditar config de CC para evitar acciones de reescritura de historia sin permiso explícito.
- **Smoke E2E que bypassa `_parse_workbook`** no detecta bugs de la UI Streamlit. Si el smoke pasa pero la UI falla, es un test gap. Considerar smoke complementario via Streamlit subprocess.
- **Spec con datos numéricos sin verificar** lleva a CC a documentar mentiras. Pre-validar contadores (`_LABEL_ALIASES`, `_ALIASES`, `_ENUM_VALUE_MAP`) antes de redactar mega-prompts.

### Próximo paso M27
M27 v1.1 funcional al 100%. Bloque opcional pendiente: B6-b-5 audit Opus + mitigaciones (30-45 min). Decisión de Lenin sobre cuándo hacerlo.

Resolver deuda P1 NUEVA (Python 3.14 + openpyxl + xlsm) antes de retomar M27 si se trabaja con archivos .xlsm reales de clientes.

Wikilinks: [[M27-flat-file-migrator]] [[M27-b6-plan]] [[STATE-agencia]] [[code-reviewer]] [[2026-05-22]]

## M29 Proposal Studio

### Foco de la sesión

Chat #1/4 del día (multi-frente). Implementación de D2 del UI dispatcher B7
en modules/pages/proposal_studio.py — primera mitad del bloque "UI dispatcher
B7 (V3+V4)" del plan al ship 28/05. Sesión partida en 4 sub-bloques: D1
discovery read-only, D2 skeleton + preview, D3 apply + save, D4 hardening.
Cerrados D1 + D2; D3 + D4 quedan para martes 26/05.

### Commits del frente

- `9c09569` — checkpoint: antes de UI dispatcher B7 V3+V4 (chat #1/4 — m29 25/05)
- `093e686` — feat(M29): B7 UI dispatcher D2 - expander + uploader + ImportReport preview (+168 LOC en proposal_studio.py)

NOTA cross-frente: el commit `252f286` con mensaje "feat(M27): B6-b-4
rollback de renderer no usado en flat_file_migrator" fue commiteado desde
este chat por error. El cambio era working tree pre-staged del chat #2 M27
que quedó atrapado bajo un mensaje M29 inicialmente (commit original
`64e3d6f` con mensaje M29 + contenido M27, amendeado al mensaje correcto
M27 antes de detectar la mezcla de frentes). El consolidador #5 debe
verificar con el chat #2 si el contenido (-147 LOC en flat_file_migrator.py)
era intencional o accidental.

### Decisiones cerradas

1. **Catalog crudo (no lookup) para `b7_importer`** — `extract_blocks` y
   `merge_blocks` esperan el dict crudo del catálogo (keys top-level
   `modules`, `tiers`, etc), NO el lookup `{module_id: mod_def}`.
   `_load_catalog_cached()` es el helper correcto.
2. **Persistencia post-merge: pasar `MergeResult.proposal_updated` tal cual a
   `pp.save_proposal`** — la función auto-bumpea version e ignora el `version`
   del input. Cero manipulación manual de versionado. Naming en disco:
   `<uuid>__v<N>.json`.
3. **Punto de inyección UI: expander colapsado en `_render_detail_screen`**,
   entre `st.divider()` y `_render_blocks_section(proposal)`. Descartadas las
   alternativas de uploader por-bloque (rompe arquitectura del merge) y top
   bar (rompe patrón visual del header).
4. **D2 NO incluye apply ni save** — solo skeleton + preview readonly del
   ImportReport. D3 (próximo sub-bloque) incluye botón aplicar + confirmación
   2-clicks + `pp.save_proposal`.
5. **Skip del bonus `b7_sample_duplicate.html`** en smoke UI del 25/05 — el
   fix D6 ya está cubierto por 3 tests pytest verdes. Se valida en E2E con
   HTML real de Ramiro pre-ship.

### Smoke E2E con fixture canónica

Drag `tests/fixtures/b7_sample_v3v4.html` (2971 bytes, del 21/05) en
`_DEMO_AgencyOS` (v12, launch/es, 20 blocks) → output confirmado visualmente:

- 3 metrics: Blocks=2 · Warnings=6 · Errors=0
- Tabla amarilla con 6 warnings cubriendo 5 codes distintos del contrato §6
  (coercion_failed×2, field_unknown, array_empty, enum_unknown, required_missing)
- 2 cards verdes "✓ aplicará" para V3_seo_opportunity y V4_listing_improvements_current_state
- Expander debug colapsado con data hidratada
- Footer naranja "✅ Report válido. El botón Aplicar merge llega en D3"

### Bugs / gotchas nuevos descubiertos

1. **Fantasma de edits perdidos en CC** — primera pasada de D2: CC reportó 5
   checks OK + Lenin ejecutó smoke exitoso (screenshots con counts 2/6/0 +
   cards verdes), pero al ir a commitear `Select-String` devolvió 0 hits y
   `git show` confirmó que el archivo no tenía los cambios. Causa raíz no
   identificada — hipótesis: el commit `64e3d6f` (mal etiquetado M29 con
   contenido M27) capturó por error los cambios pre-staged del chat #2, o
   Streamlit hot-reload corrió contra un buffer en memoria sin write-through
   a disco. Recovery: re-aplicación limpia + verificación manual triple
   (Select-String + git status + git diff --stat) antes del commit.

2. **Regla nueva pre-commit en multi-frente** — la regla "git add
   path-específico" NO es suficiente cuando hay cambios pre-staged de otros
   frentes en working tree. Siempre correr `git status` ANTES del
   `git commit` y verificar literalmente que la sección "Changes to be
   committed" contiene SOLO los paths del frente actual. Fix aplicado en
   este cierre: `git status` obligatorio post-add con check de paths
   esperados antes del commit.

3. **Coordinación chat-Lenin en comandos destructivos** — Claude tiró 4
   comandos juntos para diagnóstico pero Lenin ejecutó `git commit --amend`
   antes de ver el output. Regla nueva: comandos que mutan estado (commit,
   push, reset, amend, checkout) solo se ejecutan después de que Claude vea
   el diagnóstico previo y mande el plan explícito.

4. **Untracked basura post-cierre** — quedaron en working tree:
   - `_debug_v11.py` (luego desapareció — chat #2 lo limpió mid-session)
   - `-` (archivo literal "guión" — basura segura)
   No tocados por este frente. Anotados para el consolidador.

### Pendientes para mañana (martes 26/05)

- **D3** — Apply + 2-clicks + save del UI dispatcher B7. Estimación 60min.
- **D4** — Hardening + smoke E2E final (b7_sample_duplicate.html + HTML
  inválido para error path). Estimación 40min.
- Pivot a bloque "Refactor DataDive parsers → modules/parsers/ + mapper V3"
  del plan al 28/05 (3-4h restantes del martes).

### Wikilinks

- [[arranque-m29]] — arranque actualizado al cierre de esta sesión
- [[2026-05-22]] — sesión anterior M29 (cierre B7 Importer v1)
- [[2026-05-22-ramiro-b7-sync]] — acuerdos V3/V5 + roadmap al 28/05
- [[contrato-importer-b7-v1]] — contrato B7 draft

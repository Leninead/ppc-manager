---
tipo: daily
actualizado: 2026-08-03
---

# 2026-08-03 — Reestructura de vault pre-onboarding

> Consolidado con retraso el 14/08, **reconstruido del `git log`**. Tercer frente huérfano
> detectado por la señal B del barrido de cierre. Los 9 commits (`cfa77d4`..`5d7c70e`) estaban
> en `main` desde el 03/08; el vault no tenía ni daily ni sección de STATE. El único rastro
> era una cláusula incidental en `daily/2026-08-04.md` ("ya contenía los 16 commits de
> ordenamiento del vault"), dicha al pasar para explicar por qué un merge dio fast-forward.

**Escala: 9 commits · 74 archivos · +390 / −10.489.** Es el día que le dio al repo la forma
que tiene hoy.

## Qué se hizo

Reconstruido de los mensajes de commit, que son descriptivos. **Lo que sigue es el QUÉ; el
PORQUÉ del criterio de audiencia no está documentado — ver pregunta abierta al final.**

| Commit | Qué |
|---|---|
| `cfa77d4` | Estructura de orden **pre-onboarding**: `_archive`, SOPs por audiencia, READMEs índice |
| `892004f` | Sacar del tracking **9 archivos de ruido** de la raíz (backups, data cruda, deliverables) → `_staging` |
| `f68b02e` | Mover **11 docs históricos** de la raíz → `_archive/docs` (SOPs superados, duplicados, `TRASPASO_MottBow`) |
| `e89fc4c` | **Split de 21 SOPs** en `dev/` · `usuario/` · `agencia/` por audiencia |
| `fdc9030` | Mover `lanzamiento-non-branded` y `reportes-hostinger` de raíz → `sops/agencia` (kebab-case) |
| `0f349ed` | Consolidar la **deuda P2 de Supabase RLS en el ítem #19** del STATE (índice de menciones M28/M29/M31) |
| `275cc1b` | Consolidar **optipet en `brands`**, plan M29 en `modules`, reunión-edu en `meetings` |
| `86ff08e` | Archivar arranques duplicados obsoletos + `prompt_update_MB` → staging |
| `5d7c70e` | **Preservar el know-how de deploy de Streamlit Cloud** en `notes/knowledge/` antes de archivar `setup_deploy.py` |

El `−10.489` no es borrado de contenido: es material que **salió del índice** (destrackeo a
`_staging`) y movimientos a `_archive`. Nada se perdió, pero **no hay nota que liste qué se
destrackeó** — si algo salió por error, hay que reconstruirlo del `git log`.

Vale destacar `5d7c70e` como buena práctica: antes de archivar `setup_deploy.py` se extrajo su
conocimiento a [[2026-08-03-deploy-streamlit-cloud-original]]. Archivar sin perder el know-how.

## Por qué existió este día: es el antecedente del onboarding

**Tres de los commits dicen literalmente "pre-onboarding".** Este día no fue una limpieza
espontánea: fue la **preparación del repo para que entrara alguien de afuera**.

```
03/08 — se ordena el repo para que sea legible por un dev nuevo
06/08 — entra Juan Vargas (Write en GitHub, repo pasado a privado)
```

Es un solo arco de dos tiempos, y **ninguno de los dos estaba registrado** — el 06/08 se
consolidó el 14/08 y este el mismo día. `SETUP-DEV.md` (creado el 06/08) sólo tiene sentido
sobre la estructura que se armó acá.

## ⚠️ Pregunta abierta — el criterio del split `dev` / `usuario` / `agencia`

**No está documentado en ningún lado, y Lenin no lo recuerda.**

El commit `e89fc4c` movió 21 SOPs a tres carpetas por "audiencia", pero **el criterio que
define esa audiencia no quedó escrito**. Hoy nadie sabe a qué carpeta va un SOP nuevo.

Los casos límite son reales, no hipotéticos:
- Un SOP de flujo git que usa Lenin, ¿es `dev` (es técnico) o `agencia` (es proceso interno)?
- Un SOP de uso de un módulo que lee un AM, ¿es `usuario` o `agencia`?
- `consolidador-fin-dia.md` está en `dev/` — ¿por ser técnico, o porque el consolidador es
  un rol de dev?

**NO se inventa un criterio acá.** Queda por definir cuando aparezca la duda concreta, y ahí
se documenta de una vez. Registrado como deuda técnica **#23** en STATE-agencia.

## Nota de contexto

El commit `0f349ed` de este día es el que consolidó el ítem **#19** (Supabase RLS + anon key).
Ese mismo ítem se actualizó el 14/08 al elevarle la urgencia por el onboarding de Juan — o
sea, se estuvo editando el resultado de un día cuyo registro no existía.

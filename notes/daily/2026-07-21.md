---
tipo: daily
actualizado: 2026-07-21
---

# 2026-07-21

## Frente M24 — Innovation Board (F1 + F2)

Nuevo módulo dentro de Knowledge Base (M24): board de ideas del equipo con votación,
prototipos HTML y pipeline de estados. Worktree nuevo `C:\proyectos\ppc-manager-innovation`,
branch `feature/m24-innovation-board`.

**F1 — ideas + votos** (`d8be8d3`)
- `core/innovation_persistence.py` NUEVO (646 líneas). Réplica fiel del patrón de
  `forecast_persistence`: backend dormido opt-in (`AGENCY_OS_INNOVATION_BACKEND`),
  `_LocalBackend` (JSON en `data/innovation/`), `_SupabaseBackend` PostgREST con
  transport inyectable, cache + invalidación. 13 helpers públicos.
- `scripts/sql/m24_innovation_schema.sql` NUEVO (119 líneas, NO ejecutado). 4 tablas,
  FK on delete cascade, `unique(idea_id, votante)`, índices, **RLS activada desde el
  día uno** con policy `service_role` — no repetir el ciclo disable/re-enable de
  `forecast_clients`.
- `knowledge_base.py`: 3 edits quirúrgicos (firma `render(user_name, user_slug)`,
  tercera tab, `with tab3:`) + `_render_innovation_board` module-level. Tab1/tab2 y
  `_parse_md_file` intactos.
- `app.py`: 1 edit — `render_knowledge(_name, _username)`. Identidad por parámetro,
  NO se tocó el bloque de auth ni se stasheó en session_state.
- Voto con razón obligatoria (patrón InnovationCast): sin razón no persiste.

**Fix + smoke** (`17143e2`)
- `_USUARIOS` hardcodeada (con nombres inventados) reemplazada por `_ib_usuarios()`
  que lee `st.secrets["credentials"]["usernames"]` con fallback.
- `tests/test_knowledge_base_smoke.py` NUEVO — AppTest headless, sin `streamlit run`.

**F2 — prototipos + comentarios + pipeline** (`3707621`)
- Prototipos HTML versionados: uploader con guarda de 2 MB + anti-duplicado por firma
  `name:len`. Render con `st.components.v1.html` **fuera del popover**, en flujo
  principal vía `session_state["ib_active_proto"]`.
- Comentarios con destacado, toggle visible solo para el dueño de la idea.
- Pipeline `nueva → en revisión → aprobada → descartada` con `on_change` callback.

**Verificación:** 718 passed, 24 skipped. Mismos 4 failed + 2 errors pre-existentes
(M29/proposals/datadive, `data/sales/proposals/` seed ausente en worktree fresco).
Cero regresiones. Nunca se levantó `streamlit run`.

**Pendiente:** 3 commits locales esperando push del consolidador. Swap-day Supabase
(ejecutar SQL + flag `innovation_backend`). F3-F5 sin arrancar.

## Wikilinks
[[STATE-agencia]] · [[m24-innovation-board]]

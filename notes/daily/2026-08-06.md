---
tipo: daily
actualizado: 2026-08-06
---

# 2026-08-06 — Onboarding Juan Vargas + incidente prod (RAM) + hardening de accesos

> Consolidado con retraso el 14/08. El código de este día ya estaba en `main` desde el 06/08
> (`f4bccab`, `525ee6d`, `ca4b561`, `18ecf52`, `c809f96`); lo que faltaba era el registro.

## Onboarding — Juan Vargas

Primer dev que se suma al repo del Agency OS.

- **GitHub:** usuario `VargasJuan5826`, permiso **Write** sobre el repo.
- **Streamlit Cloud:** el permiso de GitHub es lo que le da acceso a **gestión y logs** de la app. No es un acceso separado — se hereda del repo.
- **Credenciales de app:** Juan tiene las suyas, gestionadas en **Streamlit Secrets**. *(El valor no se registra acá: el vault se pushea a GitHub.)*
- **Repo pasado a privado** como parte del onboarding.

### Tareas semana 1
1. **Ponerse al día** — `SETUP-DEV.md` + `README`, montar el entorno local y correr la baseline de tests.
2. **LIDERAR la propuesta de migración de infraestructura** — VPS vs PaaS, con destino **Freddy**. No es una tarea de ejecución: es de análisis y recomendación.
3. **Analizar los logs de RAM** — input directo para (2).

El orden no es casual: (1) le da contexto del stack, (3) le da la evidencia, y con eso escribe (2).

## Incidente prod — la app se cayó

**Síntoma:** la app dejó de estar disponible (resource limits / memory).

**Apagón visible → RESUELTO.** Lo que tiró la app hacia afuera fue el **permiso GitHub↔Streamlit al re-clonar**. Eso está arreglado.

**Causa de fondo → ABIERTA.** El problema real es el **techo de ~1 GB de RAM de Streamlit Community Cloud**, y **no hay upgrade de tier disponible**. O sea: no se compra la salida. Quedan dos caminos, y son los únicos dos:

- **optimizar** el consumo dentro del gigabyte, o
- **migrar** a otra infra.

**Fix parcial de runtime:** commit `6ded2cb` del **10/08** — lazy imports de `anthropic`/pdf + `max_entries` en los caches de upload. Baja el piso de memoria, **no mueve el techo**.

**La cadena completa, explícita:**

> incidente (app caída) → causa de fondo (techo 1 GB sin upgrade posible) → **por eso** la propuesta de migración VPS vs PaaS que lidera Juan

Esa es la razón por la que la tarea (2) de Juan existe. No es un proyecto de mejora opcional: es la respuesta a un límite duro que ya nos tiró la app abajo una vez.

## Aprendizaje operativo — concurrencia sobre el principal

**No pueden trabajar dos chats commiteando sobre el repo principal al mismo tiempo.** Falla con `claude.exe in use`.

Con Juan en Write esto deja de ser un problema de un solo operador: hay que **coordinar el no-solapamiento entre worktree y principal**. Antes bastaba con acordarse; ahora hay dos personas que pueden estar tocando lo mismo sin saberlo.

## Commits del día (ya en `main` desde el 06/08)

| Commit | Qué |
|---|---|
| `c809f96` | `SETUP-DEV.md` — guía de entorno para dev nuevo (python 3.12/3.11, secrets, baseline, git en Windows) |
| `18ecf52` | `gen_secrets`: `cookie.name` faltante (evitaba `KeyError` en boot) + guard anti-sobrescritura + stdout UTF-8 |
| `525ee6d` | pin exacto de numpy/pyarrow/anthropic/plotly/requests/dotenv/bs4 — build determinista |
| `ca4b561` | baseline vigente de tests en Setup local (866/5/9) + corregir "no hay test suite" en CLAUDE.md |
| `f4bccab` | refresh `arranque-universal` v1.1 — paths sops/dev, tabla de frentes reales, M&B cobertura |

Los tres primeros son, en la práctica, **el kit de onboarding**: sin ellos un dev nuevo no levanta el entorno.

## Pendientes de seguridad generados

Los tres van a **deuda técnica del STATE** como riesgo vivo, no acá:

- **Branch protection en `main`** (nuevo) — Juan tiene Write y hoy nada impide un push directo.
- **Auditar el historial del repo por secrets** (nuevo) — el repo estuvo **público** antes de hoy.
- **Rotar la anon key de Supabase** — sube de urgencia: al gestionar la app, Juan ve los Secrets de prod.

---
fecha: 2026-05-19
sesion_anterior: 2026-05-18
modulos_touched:
  - M27-flat-file-migrator
commits_del_dia:
  - 7aac33c  # checkpoint pre-M27 B4a
  - 9c85e06  # M27 v1.1 B4a _parse_valid_values
  - c11faf0  # M27 v1.1 B4b _ENUM_VALUE_MAP + _build_value_map
  - 07e79fb  # M27 v1.1 B5-a _locate_template_headers (D1+fallback)
  - 64f25db  # M27 fix mitigaciones post-audit B5-a
branch: main
push_status: pusheado_al_cierre
---

# Daily 2026-05-19 — M27 v1.1 B4 cerrado + B5-a con audit

## TL;DR

5 commits sobre `flat_file_migrator.py` (+365 LOC). Cerrado el bloque B4 (B4a + B4b) y arrancado B5-a (header locator) con audit code-reviewer verde APPROVE WITH CONCERNS. Progreso M27 v1.1: 62% → 75%. Próximas 2-3 sesiones para B5-b/B5-c + B6 = v1.1 end-to-end.

## Trabajo del día

### Bloque B4 — Valid Values parser + Enum value translator

**B4a (9c85e06, +39 LOC)** — `_parse_valid_values(wb) -> dict[str, list[str]]` parsea la hoja "Valid Values" del workbook. Schema-agnostic (mismo formato wide en fptcustom y PTD). Strippea sufijo categoría `" - [ COAT ]"` / `" - [ coat ]"` / `" - [  ]"` con regex tolerante a corchetes vacíos. Constante `_VALID_VALUES_SUFFIX_RE`. 4/4 asserts verdes contra fptcustom (Update Delete + Brand Name) y PTD (Listing Action + Brand Name).

**B4b (c11faf0, +130 LOC)** — `_ENUM_VALUE_MAP` (3 enums críticos hardcoded) + `_build_value_map(old_vv, new_vv) -> tuple[dict, list[str]]` + `_DEPRECATED_OLD_ENUMS` frozenset.

Decisión de scope conservadora (A1+/B2/C1):
- **A1+**: cobertura de 3 enums con mapping 1:1 identidad o rewording confirmado (`Update Delete` ↔ `Listing Action`, `Parentage` ↔ `Parentage Level`, `Product ID Type` ↔ `Product Id Type`) + set `_DEPRECATED_OLD_ENUMS` con los 2 enums OLD sin contraparte PTD (`Relationship Type`, `Variation Theme`) para que B5 emita diagnósticos diferenciables.
- **B2**: `ISBN` → `None` (deprecated). Capybaras no maneja books, mapear ISBN→GTIN automático sería perpetuar errores de cliente.
- **C1**: `GCID` → `None` (deprecated). Lógica fallback row-level vive en B5, no en B4b.

`_build_value_map` valida que old_label/new_label/values declarados existan en valid values reales (de B4a), acumula warnings sin raisear. 4 asserts verdes + 0 warnings (las 2 templates de discovery son test cases limpios).

### Bloque B5-a — Header locator con D1+fallback

**B5-a (07e79fb, +191 LOC)** — `_locate_template_headers(wb)` identifica las rows estructurales de la hoja "Template" usando estrategia D1+fallback (auto-detección row-by-row + safety net hardcoded por schema).

Hallazgo crítico del discovery: asimetría OLD/NEW en headers.
- **fptcustom**: 3 rows header (row 1 settings, row 2 display, row 3 field_ids), data desde row 4.
- **PTD**: 5 rows header (row 1 settings base64, row 2 banner, row 3 group banners sparse, row 4 display, row 5 field_ids), data desde row 6.

Esa asimetría empírica justifica D1+fallback sobre D2 hardcoded puro: si Amazon cambia row count, auto-detección absorbe el cambio antes de caer al fallback.

Constante `_TEMPLATE_HEADER_FALLBACK` tabula los offsets conocidos. 2 helpers privados (`_looks_like_field_id`, `_looks_like_display_label`) detectan patterns:
- field_id: snake_case, bracketed `[marketplace_id=...]`, prefix `::`, hash indexing `#N.field`
- display: Title Case con espacios

Cross-validation con B2 (`_parse_data_definitions`): al menos 3/5 sample field_ids de la row detectada deben aparecer como keys en dd. Si falla, cae a fallback.

`detection_method` retornado como key del dict para que el caller sepa si auto o fallback se activó (señal de regresión potencial futura).

### Audit code-reviewer B5-a

Invocado el agente `code-reviewer` (Opus 4.7) post-commit con prompt focused.

**Veredicto: APPROVE WITH CONCERNS** — 0 bugs activos, 3 P1 (riesgos robustez), 2 P2 (edge cases con saving grace), 2 P3 (no-issues).

**Mitigaciones aplicadas (64f25db, +6/-1)**:
- P2 #4 — Guard `r >= 2` en walk-up del banner detection (evita pickear row 1 settings si display_row se desplazara).
- P1 #3 — Docstring nota sobre comportamiento fail-closed cuando `dd == {}` (decisión by-design documentada).

**Deuda dejada documentada (NO aplicada)**:
- P1 #1: falsos positivos en `_looks_like_field_id` con strings tipo "Color [Required]". Mitigado hoy por B2 cross-validation.
- P1 #2: cross-validation frágil ante desalineamiento DD vs Template (sufijos PTD). Hoy no se manifiesta. Mitigación futura: normalizar via `_normalize_field_id` antes de comparar.
- P3 #7: Colisión naming `_looks_like_field_id` (singular, scalar) vs `_looks_like_field_ids` (plural, list) del porting legacy. Refactor estético, sin urgencia.

## Decisiones cerradas

- **`_ENUM_VALUE_MAP` scope conservador (A1+/B2/C1)** confirmado tras pregunta "M27 = solo Marcos o equipo amplio?" → "Por ahora solo Marcos, expansión futura posible". Razón: scope conservador escala bien (agregar entries es trivial), agresivo no escala (revertir mapping silencioso problemático cuesta).
- **D1+fallback sobre D2 puro** para header locator. Justificación empírica: la asimetría OLD/NEW (3 vs 5 header rows) prueba que Amazon NO es consistente entre schemas → auto-detección compra resiliencia.
- **CC directo sin sub-agente** para B5-a. `ppc-module-builder` evitado (alucinó 4 veces previas). `code-reviewer` aplicado POST-commit como audit estático, no DURING como executor — patrón validado útil.

## Lecciones M27

- **Antes de hardcodear value mappings, ver data real.** El spec del 18/05 asumía 5 enums con mapping 1:1. La realidad: 3 mapean limpio (1 con rewording), 2 están deprecated en PTD (`Relationship Type`, `Variation Theme`). Adaptar `_ENUM_VALUE_MAP` a la realidad evita debt técnico desde día 1.
- **Discovery scripts ad-hoc en chat ahorran sub-agentes.** 4 scripts de discovery (~30 LOC cada uno) corridos directo en PowerShell extrajeron exactamente la info que B4a/B4b/B5-a necesitaban — sin invocar agentes pesados. Patrón replicable para Bloques C* futuros.
- **Audit post-commit > audit during-commit.** code-reviewer en read-only sobre commit ya hecho da audit limpio + no interfiere con CC. Si encuentra issues, fix en commit aparte. Patrón validado en M28 (09/05) y replicado hoy.
- **detection_method como key del dict es comunicación gratuita.** B5-b/B5-c o usuario futuro puede saber si auto o fallback se activó sin instrumentación extra. Costo cero, valor diagnóstico alto.

## Deuda nueva descubierta

- **B5-a-bis (P3 #7)** — colisión naming `_looks_like_field_id` (B5-a, scalar) vs `_looks_like_field_ids` (porting legacy L910, list). Sin colisión técnica, sí confusión visual. Refactor estético, prioridad baja.
- **B5-a-bis-bis (P1 #1)** — falsos positivos en heurística `_looks_like_field_id` con display labels exóticos tipo "Color [Required]". Mitigado por cross-validation B2 hoy. Refinar regex si aparece caso real.
- **B5-a-bis-tris (P1 #2)** — cross-validation frágil ante sufijos PTD. Mitigación futura: aplicar `_normalize_field_id` antes de comparar field_ids contra B2.
- **CLAUDE.md performance flag** — CC reportó "Large CLAUDE.md will impact performance (108.9k chars > 40.0k)". Deuda blanda, deferrable hasta sesión de limpieza vault.
- **Python 3.14 + openpyxl read_only `ValueError: I/O operation on closed file`** en cleanup del interpreter. Cosmético, no afecta funcionalidad. Workaround: omitir `wb.close()` explícito en scripts ad-hoc (helpers ya cumplen caller-managed-wb pattern).

## Próximo paso M27

**B5-b — Row extractor + initial validator**. Helper que toma `wb` + headers (de B5-a) y extrae cada row de data como dict `{field_id: value}` + emite warnings por Required vacíos. ~80-120 LOC esperadas. Discovery previo: ninguno necesario (B5-a ya dio headers, B2 ya dio Required flags por field).

**Después B5-c** — Row migrator core: toma `old_row_dict` + `field_map` (B3) + `value_map` (B4b) → `new_row_dict` + diagnostics. Maneja deprecated enums, defaults para PTD-only required fields, numbering issue heredado.

**Después B6** — UI integration con M27 v1 existente. Decisión arquitectónica pendiente: tabs nuevos, toggle modo v1/v1.1, o reemplazo. NO arrancar hasta tener B5 completo.

## Cierre de sesión

- Commits del día: 5 (todos M27).
- Branch main ahead 7 commits vs origin pre-push (3 nuestros M27 + 2 M29 paralelo + 1 docs cierre 18/05 + 1 checkpoint).
- Working tree clean al cierre.
- Estado salud proyecto: ✅ verde.

## Wikilinks

[[M27-flat-file-migrator]] · [[STATE-agencia]] · [[2026-05-18]] · [[code-reviewer]]

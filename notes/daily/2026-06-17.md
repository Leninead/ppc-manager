---
tipo: daily
actualizado: 2026-06-17
---

# 2026-06-17 — Wiring Supabase Account Health: capa backend-swap + M30 en prod (dormido)

> Frente único. Branch feature/supabase-account-health → merged a main (ff). HEAD faf1ab6.

## TL;DR
Capa de persistencia de Account Health vinculada a Supabase replicando el patrón M29, pero a nivel de la capa COMPARTIDA core/persistence.py (un solo swap sirve M28+M30+futuros). M30 verificado end-to-end contra Supabase real. Mergeado a main DORMIDO: sin flag, todo sigue local (cero regresión). El swap real se activa con un flag en secrets de Cloud.

## Arquitectura (3 decisiones cerradas)
- **Backend-swap interno** en core/persistence.py: clase _LocalBackend (código Parquet histórico) + _SupabaseBackend nuevo + selector _get_backend() singleton. Los helpers públicos quedan wrappers finos, MISMAS firmas → módulos NO se tocan.
- **jsonb genérico**, 2 tablas: ah_snapshots PK (area,cliente,modulo,period) + ah_configs PK (area,modulo,name,version). Serialización DataFrame↔jsonb vía to_json/read_json(orient="table"). _history derivado on-the-fly (no se almacena).
- **Activación opt-in** (NO auto-on-creds, a diferencia de M29): _SupabaseBackend solo si hay creds Y flag backend="supabase" (env AGENCY_OS_AH_BACKEND gana, si no st.secrets["supabase"]["backend"]). Default = local. Razón: desacopla merge de activación; permite mergear código sin tocar prod hasta crear tablas y prender flag en Cloud.

## Ejecución (Fase 1 en 3 bloques, 3 commits)
- f8ea195 refactor: extraer _LocalBackend + selector (cero cambio comportamiento)
- e24ff24 feat: _SupabaseBackend + _PersistenceTransport (copia adaptada del de M29) + selector opt-in
- faf1ab6 test: 18 tests sin red, fake transport (round-trip 47/15 cols con NaN+None, eq exacto con guiones, 3 ramas del selector)
Suite: 327 passed / 3 known-supabase-env failures de M29 (baseline rojo aceptado, ver deuda).

## Supabase (capybaras-os-prod, mismo proyecto que M29)
- Tablas ah_snapshots + ah_configs creadas vía DDL en SQL Editor. RLS OFF (idempotente, confirmado relrowsecurity=false).
- 401 en primer write: era RLS activo (el disable inicial no había tomado / verificación visual saltada). Diagnóstico: grants de anon estaban OK (INSERT/UPDATE presentes), causa = RLS. Re-disable resolvió. NO se usó service_role (mantener anon + RLS off, coherente con M29 y con deuda P2).
- Verificado end-to-end en LOCAL con flag activo: write/read/list/config reales contra Supabase. Datos de prueba (_smoketest/2026-W99) borrados (DELETE 204, verify 0).
- Flag revertido en local post-verificación (dev no debe escribir a prod).

## Migración: N/A
Inventario confirmó 0 datos M30 locales (la carpeta data/account-health/pricing-dashboard/ nunca se creó — M30 validado pero nunca persistió snapshot real). Tablas arrancan vacías; primer snapshot lo crea el AM al importar en Cloud.

## Pendiente
- **Swap-day Cloud** (3 min, cuando se quiera activar): agregar backend = "supabase" a [supabase] en secrets de Streamlit Cloud + REBOOT manual. Ahí M30 persiste en Supabase.
- **M28** (lunes): más pesado que M30. 4 escrituras fuera de banda — tracked-skus.json (json directo, no _save_config), shutil.rmtree L223 + 2 unlink L1431/L1439 (no existe _delete_snapshot en core/persistence), + 2 Path("data") relativos en vez de DATA_ROOT. Requiere agregar helpers de borrado a la capa + logs (ah_logs). ~2-2.5h.

## Deuda registrada
- **Test-isolation M29** (P2): 3 tests rojos porque secrets.toml tiene creds vivas y los tests M29 nunca se aislaron de secrets (auto-on-creds los manda a red real). Fix: monkeypatch env+secrets en esos 3. Sesión aparte, NO bloqueante (M29 prod funciona). Norte: migrar M29 a opt-in como Account Health.
- **.gitignore**: endurecer .streamlit/secrets.toml → .streamlit/secrets.toml* (cubre .bak/.old futuros). Commit de 1 línea, aparte. Hoy el .bak se movió fuera del repo (C:\proyectos\secrets.toml.bak).
- **anon key P2** (ya registrada): RLS off + anon full access, tolerable single-tenant. Antes de multi-tenant/exposición: service_role + RLS con políticas.

## Wikilinks
[[STATE-agencia]] · [[m30-pricing-dashboard]] · [[m29-proposal-studio]]

---

## LTD (frente)

> Frente en paralelo (Setex agrega la suya). Detalle completo en [[LTD]].

- **Meeting dueño (Aaron/Adam):** caída de ventas validada como **precio, NO PPC**. Semana -24.6% sales, tráfico plano (-4.1%), ACoS 19.8% en target; lo único que se desplomó fue conversión 3.28%→2.37% (-27.7%). Causa = precio post-Hot Sale (SQP: relación precio↔share r=-0.80).
- **Bulk F0 aplicado HOY (Success):** pausadas 3 camps con gasto y 0 ventas (SwaddleMe conquest, TOP20 conquest, SU-612M EXACT saco); bid-down `swaddle para bebe 0-3` $12→$8; 6 negative PT nuevos en Scavenger AUTO (incl. B0F8PCWD6J = ASIN propio, auto-canibalización).
- **Prime Day 23–30 jun AUTOMATIZADO (auto-revert 1/jul):** campaña Grey `SU-PUSH | MX | SP | KW | GREY | PRIMEDAY` creada (bulk F1b, Success) con start/end horneados; Budget Rules schedule-based por UI (PT Category +33% → ~$698/d, Broad +30% → $455/d).
- **Grey = B0081GIZ52** (Swaddle UP transición gris, CVR 8.25%, stock profundo) — identificación asumida del pedido del cliente vía Agustín, pendiente confirmación.
- **Pendientes (Agustín):** ETA restock B09MG1PM6L (hero #2, ~18d runway → gatilla pull-back, bulk listo); confirmar Grey=B0081GIZ52; verificar elegibilidad de los 2 product ads (fallback SKU `L20 01 002 GR M-stickerless`).
- **Aprendizajes bulk (a CLAUDE.md vía consolidador):** (1) celdas vacías de `Start Date`/`State` en filas Entity=Campaign → upload Failed, poblar con valor real aunque no se cambie; (2) Budget Rules por UI, NO por bulk (hoja BSE vacía + enums no confiables).

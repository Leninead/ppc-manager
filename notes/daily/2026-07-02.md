# 2026-07-02

## M31 Revenue Forecast — cierre de integración

MVP completo (F1→F5) integrado en `main` y pusheado (commit `88b1abe`).

- **F1→F5:** ingesta CSV/Excel, motor blend MoM/YoY + seasonality + OOS + derivación AOV/CVR, UI de forecast editable, export CSV.
- **Tests:** 187 verde.
- **Integración:** por rebase de `feature/m31-forecast-port` sobre `main` (main había divergido por M32 Fase 0).

### Deuda post-MVP
- Persistencia dormida: `_PERSISTENCE_ENABLED = False` (backend Supabase existe, apagado tras flag).
- Fixtures reales gitignored (tests contra data sintética).
- Pendiente: F6, Keepa, persistencia Supabase.

## M31 Persistencia Supabase — encendida e integrada (tarde)

Merge `ca9737d` integró `feature/m31-forecast-persistence` (`601fa6a`) sobre main.

- `_PERSISTENCE_ENABLED = True`. `_hydrate_clients` cableado en `_ensure_state` (guard idempotente). Botón "Guardar cliente" + autosave en demo-load y forecast-gen (no en cada rerun ni en el seed).
- Wrappers `_try_hydrate`/`_try_persist`: try/except → st.error, session_state intacto si el backend falla. Resiliencia validada en vivo (un 401 real no rompió la app).
- `core/forecast_persistence.py` intacto (D3 vigente, cero Account Health).
- Probado end-to-end contra Supabase real: guardar → fila en forecast_clients → recargar → hidrata. Tabla limpiada tras la prueba.
- Tests M31: 189 passed / 5 skipped.

### Deuda registrada
- RLS se reactivó sola entre Fase 1 y 3 (dejado en false, verificado pg_class; al guardar estaba en true → 401). Puede repetirse en prod. Hardening a decidir: investigar reactivación, o RLS on con política permisiva service_role.
- Flag prod NO seteado: falta AGENCY_OS_FORECAST_BACKEND="supabase" en Secrets de Streamlit Cloud. Sin eso persiste en local pero NO en prod. Creds Supabase probablemente ya presentes (AH las usa); falta el flag de forecast.
- Falta UI de creación de cliente no-demo (única entrada hoy: demo no-persistible o cargar BR al activo). Candidato a fase corta.
- M29: 2 tests de apply (test_b7_importer_apply_happy_path, test_datadive_v3_apply_path) fallan por bump v12→v13 no persistido. PREEXISTENTES (fallan igual en 88b1abe). Deuda M29 aparte, no bloqueante.

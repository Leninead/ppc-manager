# 2026-07-07

## M31 UI crear cliente — integrado a main

Merge `a110edc` integró `feature/m31-crear-cliente` (`f004189`) sobre main (base `7d1ef7e`).

- UI: popover "➕ Nuevo cliente" pegado al selector (`st.columns`, no expander — gotcha 1.43.2). Inputs name + marketplace + botón Crear.
- Helper testeable `_create_client_flow(name, marketplace, state)`: valida vacío + duplicado (case-insensitive) → `_new_client` → append catálogo → `_set_active_client` → `_try_persist`.
- FIX: `_persist_clients` ahora saltea el demo (`if id=="demo-client": continue`) → el demo NO ensucia Supabase. Registro `_meta/active` intacto.
- Cierra la deuda "falta UI de creación de cliente no-demo" (M31).
- Probado end-to-end local (Supabase real): crear → activo → fila en forecast_clients; demo NO aparece.
- 6 tests nuevos en verde (test_revenue_forecast_crear_cliente.py). Suite completa: 580 passed, 5 skipped, 3 failed (los 3 preexistentes conocidos: 2 M29 apply + 1 entorno; ninguno nuevo).

### Deuda / pendientes
- Flag prod: `forecast_backend = "supabase"` agregado a Secrets de Streamlit Cloud — VERIFICAR que quedó guardado. Tras redeploy, probar crear cliente en capybaras-os.streamlit.app confirma flag + código en prod.
- RLS: sigue la deuda de reactivación automática (hardening pendiente, no bloqueante).
- Menor: warnings LF→CRLF → considerar .gitattributes.

### Otros hoy (contexto)
- M32 PDF step-fix pusheado a prod esta mañana (7d1ef7e): steps del PDF en naranja plano, arreglado el círculo roto de xhtml2pdf.

## M28 SKU Progress — categorización de eventos, integrado a main

Merge `b01df9d` integró `feature/m28-sku-detail-view` (4 commits, `c98bc83`..`4ef3268`) sobre main. Auto-merge limpio pese al desfase de un mes (branch cortado en fe86bd3, 2026-06-01) — M28 solo tocó sku_progress_report.py, sin colisión con M30/M31/M32.

- Bloque 1: campo `category` en eventos de optimización (constantes + append + coalesce en lectura, sin migración de parquet).
- Bloque 2: selector de categoría en modal + color de chip por categoría.
- Bloque 3: color por categoría en las vlines de los 5 charts.
- Helper puro `_coalesce_category`: rellena category ausente/nula/vacía con "Sin categoría" en lectura (filas viejas se completan al cargar).
- Set cerrado de 7 categorías: Main Image, Imágenes secundarias, Título, Bullets, A+ Content, Precio, Otro.
- 3 tests nuevos verde (test_sku_progress.py). Smoke visual verificado con CSV real (Dermaglos W28): chip + vlines coinciden por categoría en los 5 gráficos.
- Suite completa post-merge: 583 passed, 5 skipped, 3 failed (los 3 preexistentes; Account Health intacto: 58 tests verde).
- category seguro bajo Supabase sin migración: ah_logs.data es jsonb, absorbe el campo nuevo.

### Corrección M32 (deuda saldada)
La deuda "biblioteca M32 no persiste en prod" queda SALDADA. Verificado empíricamente: un caso guardado sobrevive reboot completo de la app (borra el disco efímero de Streamlit Cloud) — si fuera backend local, el reboot lo habría borrado. Flag backend="supabase" activo en Cloud Secrets. M32 100% funcional en prod: 3 modos, persistencia Supabase, exports (con PDF steps arreglados), bilingüe.

### Pendientes no bloqueantes (para consolidador)
- Docstring SQL stale en _SupabaseBackend (main L461-480) omite ah_logs/ah_client_configs — cosmético, tablas ya existen en prod.
- 6 presets stale en notes/prompts/ con git add ./push viejos: startup-2026-05-21, arranque-{dermaglos,mb,ltd,optipet}, cierre-meta. Canónico: arranque-universal.md.

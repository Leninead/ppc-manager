---
tipo: daily
actualizado: 2026-06-21
---

# 2026-06-21 — M28 SKU Progress wired a Supabase: capa completa + módulo backend-agnostic, EN PRODUCCIÓN

> Frente único, sesión larga. Branch feature/m28-supabase → merged a main (ff). HEAD 28d2905.

## TL;DR
M28 quedó vinculado a Supabase end-to-end y EN PRODUCCIÓN. Extendí la capa compartida core/persistence.py (borrado + logs + client-configs + list_clientes en ambos backends) y dejé el módulo sku_progress_report.py 100% backend-agnostic (cero I/O de disco). Smoke E2E verde contra Supabase real. Mergeado a main; como el flag ya estaba activo en Cloud (de M30), M28 quedó productivo con el push — NO hubo swap-day separado.

## Arranque del día (contexto recuperado)
- M30 swap-day en Cloud: confirmado HECHO (flag backend="supabase" presente en secrets de Cloud desde el miércoles, M30 verificado activo). El flag es GLOBAL — activa la capa entera (M28+M30), no por módulo.
- Sin actividad de código entre 17/06 y hoy: solo 4 commits docs de cliente (Dermaglos/LTD/Setex). M28 no se había empezado.

## Lo que se hizo (6 commits en main, ff sobre 501a382)
- 7d2ac7f Bloque 1: borrado en la capa (_delete_snapshot/_delete_history/_delete_cliente en ambos backends, delete_cliente ACOTADO al módulo, no cross-módulo).
- f51d1d3 Bloque 2: logs append-only portados a los backends (ah_logs, INSERT no-upsert), delete_cliente encadena logs.
- 3914193 Bloque 3: client-configs per-cliente (tabla ah_client_configs PK area,cliente,modulo,name) — caso tracked-skus.json. delete_cliente encadena las 3 tablas.
- ec80e8e fix review: M1 (_load_log deja de estar cacheado — filters:dict incompatible con st.cache_data), m2 (_delete_snapshot invalida _load_history), n5 (+6 tests de wrappers cacheados).
- 20c158d Bloque 4: módulo M28 consume la capa — las 4 escrituras fuera de banda + tracked-skus.json directo → llamadas a la capa. Quita imports json/shutil.
- 28d2905 Bloque 4b: list_clientes en la capa (Local=carpeta del módulo, Supabase=union ah_snapshots ∪ ah_client_configs) — cierra la última atadura a disco del módulo (el selector escaneaba disco → en Cloud los clientes desaparecían tras reboot). + poda de _tracked_skus_path (dead code).

Suite: 367 passed / 3 known-supabase-env failures de M29 (baseline rojo aceptado, deuda vieja). 2 code-reviews (capa + módulo) ambos MERGE limpio.

## Arquitectura (decisiones cerradas)
- Backend-swap en la capa compartida, NO por módulo: un swap sirve M28+M30+futuros. Los módulos delegan en wrappers de firmas estables.
- 4 tablas Supabase (capybaras-os-prod, mismo proyecto que M29/M30): ah_snapshots + ah_configs (ya existían, M30) + ah_logs (id surrogate, append-only) + ah_client_configs (PK con dimensión cliente — la que ah_configs no tiene). Las 2 nuevas se crearon hoy vía DDL.
- delete_cliente ACOTADO al módulo (decisión de producto): borrar un cliente desde M28 NO vuela su data de otros módulos (el rmtree viejo de carpeta era bug latente). En Supabase = DELETE filtrado por modulo en las 3 tablas, encadenado.
- _load_log NO cacheado (excepción documentada): su filters:dict es incompatible con st.cache_data.

## Supabase (operativo)
- DDL ah_logs + ah_client_configs ejecutado en SQL Editor. RLS off.
- Smoke E2E M28 verde: client-config + snapshot + log(doble append) + list_clientes + delete_cliente encadenado, contra Supabase real, autolimpiado (cliente _smoketest_m28, 0 residuo).
- Flag local: activado SOLO durante el smoke, revertido al cerrar (dev no escribe a prod). El flag productivo vive en Cloud.

## Estado: M28 PRODUCTIVO
El flag global ya estaba en Cloud → el merge fue la activación. M28 escribe a Supabase en producción. Verificación visual en Cloud HECHA (2026-06-21): creado cliente `dermaglos` + SKU `PVENUS0782` en M28 Cloud → reboot → persiste. Fila confirmada en `ah_client_configs` (area=account-health, cliente=dermaglos, modulo=sku-progress, name=tracked-skus, data jsonb con sku+asin, updated_at 2026-06-22 00:48 UTC). RLS disabled confirmado visualmente en las 4 tablas AH. **M28 cerrado al 100%.**

## Deuda registrada
- **RLS off no toma de forma confiable vía DDL (P2 operativa)**: el `alter table ... disable row level security` del DDL NO quedó aplicado de forma confiable — pasó DOS veces hoy (ah_snapshots a la mañana, ah_logs+ah_client_configs a la tarde): el DDL "Success" pero RLS seguía activo → 401 engañoso en el primer write (parece auth, es RLS). Fix reproducible: tras crear tablas, correr `select relname, relrowsecurity from pg_class where relname in (...)` para CONFIRMAR relrowsecurity=false, y re-correr el disable si hace falta. NO agregar policies (no es el problema). Documentar en el playbook de creación de tablas.
- Comentario desactualizado en test_pricing_persistence.py L257 (menciona _load_log como cacheada — ya no lo está). Cosmético.
- 3 tests known-supabase-env de M29 siguen rojos (test-isolation, deuda vieja, sesión aparte).
- .gitignore: endurecer a .streamlit/secrets.toml* (deuda vieja del miércoles).

## Wikilinks
[[STATE-agencia]] · [[m28-sku-progress-report]] · [[m30-pricing-dashboard]] · [[2026-06-17]]

## 2026-06-21 (cierre tarde) — SOPs de uso embebidos in-app en los 3 módulos productivos

Después de cerrar M28 al 100%, embebí la guía de uso de cara al usuario dentro de los 3 módulos que persisten en Supabase, para uso diario sin salir de la app.

**Qué se hizo:**
- Patrón uniforme: constante módulo-level `_SOP_MD` (markdown) + `st.expander("📘 Cómo usar este módulo")` top-level apenas debajo del header en `render()`. Discovery de anclas read-only primero, 2 edits quirúrgicos por módulo.
- SKU Progress Report, Pricing Dashboard, Proposal Studio. py_compile verde, verificado en Cloud post-reboot (los 3 expanders renderizan; Proposal Studio confirmado en listado Y en vista detalle, gracias a que el expander quedó antes del early-return).
- Proposal Studio: a pedido, se dejó EXPLÍCITO que manual vs importación NO es todo-o-nada — cada bloque tiene una sola vía y se complementan: Brand/Category a mano (no se importan), SEO/Listing-estado-actual/Listing-comparativa/Plan-de-crecimiento solo por importación (readonly, vienen del HTML de Ramiro; SEO además de DataDive). Son independientes, sin orden obligatorio.

**Commits:** `2607b4e` (embed 3 módulos, +108) · `797f7eb` (Proposal Studio: manual vs importación por bloque, +9/−2). Ambos en origin/main.

**Registro:** convención SOP in-app en modules/pages/CLAUDE.md. Copias de equipo en notes/sops/SOP_USER_{SKU_Progress_Report,Pricing_Dashboard,Proposal_Studio}.md (cuerpo = el `_SOP_MD` de cada módulo).

**Wikilinks:** [[STATE-agencia]] · [[m28-sku-progress-report]] · [[m30-pricing-dashboard]]

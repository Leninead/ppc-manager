---
tipo: daily
actualizado: 2026-07-22
---

# 2026-07-22

## Frente M24 — Innovation Board F3 (pipeline visual + prioridad)

Segunda sesión del frente. F3 convierte el board de "lista votada" a herramienta de
gestión con pipeline visible y priorización explícita.

**Commit:** `a568ae1` (local, no pusheado)

**A — Schema + persistencia**
- 3 columnas nuevas en `innovation_ideas`: `asignado_a`, `razon_descarte`,
  `estado_updated_at`. Inline en el CREATE + `alter table add column if not exists`
  para DBs ya creadas (idempotente).
- `_update_idea` sella `estado_updated_at` automáticamente cuando el patch toca
  `estado` — el sellado vive en la capa de persistencia, NO se delega a la UI.
- Migración defensiva EN LECTURA: `"en revisión"` (F2) → `"en debate"` (F3), aplicada
  en `_get_idea` y `_list_ideas`. Sin script de backfill. El filtro de estado corre
  DESPUÉS de migrar, para que las ideas legacy se filtren por su estado nuevo.

**B — Pipeline de 6 estados**
`nueva → en debate → aprobada → en desarrollo → completada → descartada`
Cada uno con emoji + bg + fg en `_IB_ESTADO_META`. El orden de la lista ES el orden
del pipeline: lo consumen el Kanban y la barra de progreso.

**C — Score de prioridad 0-100**
`(votos_norm × 40) + (impacto × 35) + (facilidad × 25)`, redondeado a entero.
Votos normalizados contra el máximo del board, clampeados a [0,1] (los scores de
votos pueden ser negativos). Guarda de división por cero si `max_score <= 0`.
Umbrales: ≥75 CRÍTICA · ≥50 ALTA · ≥25 MEDIA · resto BAJA.
El orden del board pasó de (votos → cuadrante) a prioridad desc. `_ib_quadrant_rank`
y los pesos viejos eliminados sin refs colgadas.

**D — Tres vistas con toggle** (`st.radio`, no `st.tabs`: ya estamos dentro de una tab)
- Lista: cards completas con badges de prioridad y estado, asignado, fecha de estado.
- Kanban: 6 columnas, cards compactas, SIN popovers (ilegible a 6 columnas). Botón
  "Abrir" → setea focus + cambia a Lista filtrada.
- Matriz: scatter Plotly (X esfuerzo, Y impacto, tamaño=prioridad, color=estado),
  jitter DETERMINISTA por índice (sin random, para render estable), cuadrantes
  anotados, divisorias con hex de 6 dígitos.
- Barra de progreso segmentada (ancho ∝ conteo) + leyenda, visible sobre las 3 vistas.

**E — Asignación y descarte**
- Selectbox "Asignado a" con `on_change` → persiste.
- Descarte con razón obligatoria: sin razón, el cambio de estado NO se aplica.
- Aviso "⚠️ sin asignar" si el estado es "en desarrollo" o "completada" sin responsable.

**Verificación:** 726 passed (+8), 24 skipped. Mismos 4 failed / 2 errors
pre-existentes (M29/proposals, seed `data/sales/proposals/` ausente). Cero
regresiones. Nunca se levantó `streamlit run`.

## Wikilinks
[[STATE-agencia]] · [[m24-innovation-board]] · [[2026-07-21]]

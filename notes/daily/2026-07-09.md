---
tipo: daily
actualizado: 2026-07-09
cliente: dermaglos
---

# 2026-07-09 — M31 F6.1a parser por-ASIN + consolidación

## Tareas completadas

- **F6.1a — parser snapshot por-ASIN** (frente `feature/m31-forecast-asin`, worktree `C:\proyectos\ppc-manager-forecast-asin`). TDD estricto (RED→GREEN).
  - `_parse_asin_report(file_or_bytes, period)` — parsea UN CSV "Detail Page Sales and Traffic By Child Item" → lista de dicts, 1 por child ASIN. Acepta bytes / ruta / file-like. pandas + `encoding="utf-8-sig"` (mata BOM), respeta comillas (títulos con comas internas intactos).
  - `_is_asin_report(header)` — detección de formato por presencia de `(Child) ASIN`. Solo detección, sin routing.
  - Reutiliza `_parse_num` del MVP para moneda/comas/%. Vive en `revenue_forecast.py` junto al parser by-date (cohesión).
  - Shape 10 keys: parent_asin, child_asin, title, sessions, page_views, buy_box_pct, units, unit_session_pct (CVR), revenue, period.
- **Consolidación a main**: FF-merge limpio (`6df80ce..21a713c`), push a origin. Suite M31 completa 180 passed / 5 skipped. D3 cero AH.
- **Docs**: STATE-agencia + este daily + CLAUDE.md (row M31 + sesión).

## Decisiones tomadas

- **`period` como parámetro**, no inferido — el reporte NO tiene columna Date, es un snapshot del rango descargado. El mes lo aporta el usuario (F6.1b).
- **Snapshot puro, sin filtrado** — el export trae 10 filas (9 children + la fila del ASIN padre `B0FDX9XR56` como su propio child, sessions=1/units=0/rev=0). El parser las devuelve todas; el rollup padre/hijo es F6.1b+.
- **B2B cols ignoradas en v1** — match exacto de header evita confundir `Sessions - Total` con `Sessions - Total - B2B` (test dedicado).
- **BOM literal → `﻿`** en `_norm_header` (auto-documentado, robusto ante re-encode). NO se tocó el mismo patrón preexistente en L736 (fuera de scope).

## Bloqueos

- Ninguno.

## Fixture

- `tests/fixtures/real/dermaglos_asin_bychild_2026-07.csv` (Dermaglos, julio 2026, 10 ASINs) — GITIGNORED, dato de cliente, NO versionado. Identificado sin adivinar por header (`(Parent) ASIN,(Child) ASIN,Title`, 21 cols, ~10 filas) entre varios BusinessReport en Descargas.

## Próximos pasos (F6.1b+)

- Acumulación multi-mes + modelo de ASINs que junta historial por child.
- 3 niveles: child / parent (rollup) / cuenta.
- UI/tab por-ASIN (drill-down).
- Forecast por-ASIN + integración Keepa.

## Deuda menor

- BOM literal (U+FEFF) en L736 del parser by-date de `revenue_forecast.py` — micro-frente aparte, no urgente. Memoria: `bom-literal-tech-debt-revenue-forecast`.

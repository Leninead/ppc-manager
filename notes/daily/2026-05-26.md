---
fecha: 2026-05-26
sesion_anterior: 2026-05-25
modulos_touched:
  - M27-flat-file-migrator
  - M4-analisis-cruzado
  - M29-B7-dispatcher
commits_del_dia:
  - c10fa27  # chore(infra): setup venv Python 3.12 + doc setup local
  - 5fa3e02  # docs(M27): ship v1.1 soft launch + audit B6-b-5 + venv 3.12 + daily 26/05
  - b2763cb  # fix(M4): 14 bugs análisis cruzado (10 reportados + 4 audit)
  - fea9a1c  # feat(M29): B7 UI dispatcher D3
  - 416dcbb  # fix(M29): B7 dispatcher st.expander → st.popover
  - af7ea91  # test(M29): D4 fixture HTML inválido para error path
  - 6bcf7de  # test(M29): D3 smoke E2E happy path validado en worktree m29-d3
  - (merge)  # merge: M29 B7 dispatcher D3+D4 completo
branch: main
push_status: pushed  # consolidado por chat M29 (merge --no-ff feature/m29-b7-d3-d4 + push a origin/main)
---

> **Daily consolidado de 2026-05-26** — 2 frentes paralelos colisionaron en este archivo
> (worktrees-flow v1.0: 1 daily por fecha). PARTE 1 = M27 (main directo). PARTE 2 = M29
> (frente `feature/m29-b7-d3-d4`, mergeado a `main` por la sesión consolidadora M29).
> El frente M4 (análisis cruzado, `b2763cb`) fue commiteado+pusheado por su propia sesión
> y documenta aparte; queda listado arriba en `commits_del_dia` por completitud del día.

# Daily 2026-05-26 — M27 v1.1 SHIP (soft launch) + audit Opus + venv 3.12

## TL;DR

M27 Flat File Migrator v1.1 SHIPPED (soft launch, soak 14 días). Audit Opus
B6-b-5 PASS sin P1 (4 P2 docs). Setup venv Python 3.12 destrabó bug P1
segfault del 3.14 (resultó ser ABI de NumPy/pyarrow, no específico de .xlsm
con macros como creíamos el 25/05). Validado E2E contra .xlsm originales par
Gamboa. Validado visualmente en deploy live (toggle v1/v1.1 + auto-detect
funcionando). Mensaje a @channel del team enviado por Slack con SOP completo.

## Sesión M27 — paso a paso

### Paso 1 — Audit code-reviewer Opus 4.7

Audit read-only sobre delta `f71bfdf~1..b9d9264` (B6-b-1 a B6-b-4 + restore b0a234c).

Veredicto: **APPROVE WITH CONCERNS**.
- 0 P1 bloqueantes.
- 4 P2 documentadas (no aplicadas):
  * `redundant-open`: 4 aperturas de workbook por click. Mitigación futura: cachear _detect_schema por wb_bytes.
  * `no-close`: _open_ws no cierra wb. Sin handle leak, pero memoria sube si AM migra repetido.
  * `methods_count` re-derivada: SIN falso positivo confirmado por análisis. Refactor futuro: que _build_field_map devuelva el método.
  * `import-inline`: `import pandas as pd` dentro de rama diagnostics. Cosmético.
- Sin regresión sobre deuda P3 heredada (B5-a-bis, _extract_template_rows huérfano, etc.).
- 4to hit consecutivo del patrón audit post-commit estable (M28 09/05, M27 B5-a 19/05, M27 B5-b 20/05, M27 B6-b 26/05).

Decisión: no mitigar los P2 hoy. Replicar patrón "P1 se mitiga, P2/P3 se documenta".

### Paso 2 — Setup venv Python 3.12

Bug P1 a destrabar: smoke wrapper B6-b-1 crasheaba con exit code
`-1073741510` (segfault C-level, sin traceback) en Python 3.14.3 + openpyxl
3.1.5. Hipótesis confirmada después: ABI inmadura de NumPy 2.x / pyarrow
con CPython 3.14.

**Setup:**
1. `winget install Python.Python.3.12` → Python 3.12.10 instalado
2. `py -3.12 -m venv .venv` → venv creado en C:\proyectos\ppc-manager\.venv
3. `.\.venv\Scripts\Activate.ps1` → activado
4. `python -m pip install --upgrade pip` → 25.0.1 → 26.1.1
5. `pip install -r requirements.txt` → exit 0, todo prebuilt wheels, sin builds
6. Sanity imports OK: streamlit + pandas + openpyxl + pdfplumber + plotly + requests + bs4 + anthropic + dotenv
7. Smoke pipeline B1→B5-c PASS 12/12 (335ms)
8. **Smoke wrapper B6-b-1 PASS** ← el que segfaulteaba en 3.14
   - rows_count 6 · coverage_pct 42.1% · matches {label_match: 66, alias_label: 3}
   - 3 enum translators · output_rows 12 · xlsx 204553 bytes
   - Tiempo pipeline interno: ~355ms · wall-clock: 242s (dominado por compilación on-import primera vez en venv nueva)

**Documentación:**
- `.gitignore` updated: +`.venv/` +`.claude/settings.local.json`
- `CLAUDE.md` del repo: agregada sección "## Setup local (Python 3.12 obligatorio)" después del Setup viejo (no reescribimos el viejo para evitar conflicto, queda deuda P3 de consolidar)

**Commit limpio (paso 2.5):**
- `git add .gitignore CLAUDE.md` (selectivo)
- Commit `c10fa27`: chore(infra): setup venv Python 3.12 + doc setup local (destraba P1 segfault 3.14)
- analisis_cruzado.py del chat M29 quedó OUT (esperado)
- .claude/settings.local.json quedó OUT (ya estaba trackeado, gitignore no destrackea)

### Paso 2.6 — Validación contra .xlsm originales

**Descubrimiento clave del agente CC:** `scripts/smoke_b6a_e2e_pipeline.py` tiene los paths .xlsm hardcodeados en L41-42 desde siempre. El smoke del Paso 2 ya había corrido contra los .xlsm originales, no contra .xlsx convertidos como asumimos.

**Re-run explícito** contra:
- OLD: `data\account-health\flat-file-migrator\ALRBB093_p_USA_2026__1_.xlsm` (1.29 MB)
- NEW: `data\account-health\flat-file-migrator\COAT__5_.xlsm` (1.26 MB)

**Resultado:** PASS exit 0, byte-idéntico al run del Paso 2 salvo Δ1 byte de jitter del zip (timestamps internos del .xlsx output).

**Conclusión:** P1 del 25/05 cerrado al 100%. El bug era exclusivamente Python 3.14 ABI, NO específico de .xlsm con macros. La conversión .xlsm → .xlsx nunca fue el fix real; era placebo. Fix real: venv 3.12.

### Paso 3 — Ship soft launch

**Validación visual deploy live** (`capybaras-os.streamlit.app`):
- Sidebar → Account Manager → Flat File Migrator → tab USA
- Upload ALRBB.xlsx + coat.xlsx
- Toggle "Modo de migración: Same-schema (v1) / Cross-schema (v1.1)" visible ✓
- Auto-detect caption: "Detectamos schemas distintos: OLD=fptcustom → NEW=ptd. Recomendamos Cross-schema." ✓
- Cross-schema seleccionado por default ✓
- Click "Migrar Data →" → output `coat_migrated.xlsx` descargado OK ✓

**arranque-m27.md update:**
- `feature_status: activa` → `shipped-monitoring`
- `actualizado: 2026-05-25` → `2026-05-26`
- Secciones "Estado actual" + "Pendientes activos" + "Próxima sesión propuesta" + "Historial de sesiones" actualizadas

**Mensaje a @channel del team (Slack):**

Estructura:
1. Título: M27 Flat File Migrator v1.1 — LIVE
2. Saludo team + intro contexto (Amazon migrando templates a PTD, antes era a mano)
3. URL del deploy + sidebar path + crédito a Marcos por la lógica
4. SOP 8 pasos (marketplace tab → uploaders → header rows → saltar example row → toggle modo → click migrar → output descargado)
5. Diagnostics explicados (unmapped_field, missing_required_in_new, deprecated_value)
6. "Lo que tenés que saber": cobertura típica, par validado oficialmente, formatos soportados, enums traducidos vs deprecated
7. Cierre: llamado a probar + oferta de acompañamiento + ping a Marcos como piloto interno

## Lecciones del día

**Lección 1 — Pre-validar contadores Y paths en specs antes de aceptar.**
El bug del 25/05 ("convertir .xlsm a .xlsx") era diagnóstico equivocado del workaround. CC corrigió leyendo el código real (L41-42 hardcoded). Patrón a aplicar: cuando el vault dice "X es el workaround", verificar contra código antes de seguir asumiéndolo. Mismo patrón ya aplicado el 25/05 (40 vs 3 aliases).

**Lección 2 — venv pythonización ahorra horas.**
5 min de winget install + 2 min de venv = 100% confiabilidad future-proof. Documentación en CLAUDE.md previene que este P1 reaparezca a futuro o en otra PC del equipo.

**Lección 3 — Soft launch > Formal launch para módulos single-tenant interno.**
Mantiene el prompt activo durante soak (14 días) sin overhead de archivar/desarchivar. Replica patrón M28. Trigger para mover a `_shipped/`: 14 días sin bugs reportados.

**Lección 4 — 4to hit audit Opus sin P1 reales — patrón estable.**
Mantener como ritual no negociable pre-ship.

**Lección 5 — Crédito por lógica/workflow vs código.**
Marcos no escribió código pero aportó el know-how del mapeo manual. Crédito explícito en mensaje team da reconocimiento real sin atribución incorrecta. Patrón replicable para futuras herramientas que automatizan know-how de AMs.

## Deuda nueva descubierta hoy

- P3: Restringir file_uploader UI a xlsx/xlsm (formatos .xls/.tsv/.csv/.txt no validados visibles)
- P3: Consolidar 2 secciones Setup en CLAUDE.md del repo
- P3: Decidir destrackeo de `.claude/settings.local.json` con `git rm --cached` (coordinar con M29)

## Próximo paso

**Trigger-based.** Sin bloque obligatorio agendado.

Triggers posibles:
- Bug reportado por Marcos o team durante soak (urgente)
- Fin de soak ~2026-06-09 sin bugs → mover arranque-m27.md a `_shipped/`
- Caso real con formato no validado (.xls/.tsv/.csv) → ampliar testing
- Decisión estratégica de extender cobertura (otros marketplaces, otras categorías)

Wikilinks: [[M27-flat-file-migrator]] [[arranque-m27]] [[STATE-agencia]] [[code-reviewer]] [[2026-05-25]]

## Cierre de sesión

- Working tree: 1 commit creado hoy (c10fa27) + el commit de docs por crear en este paso
- Branch: main, 2-3 commits acumulados sin push (esperando coordinación con chat M29)
- Estado de salud del proyecto: ✅ verde

---

# 2026-05-26 — M29 B7 dispatcher D3+D4: smoke E2E + diagnóstico save

> **PARTE 2 — frente paralelo M29.** Worktree `C:\proyectos\ppc-manager-m29-d3`,
> branch `feature/m29-b7-d3-d4`. Esta sesión actuó de consolidadora: corrió la suite,
> mergeó `feature/m29-b7-d3-d4` → `main` (merge --no-ff) y pusheó a `origin/main`
> (cumple el `push_status: pendiente` que dejó M27 arriba).

Arranque del bloque: HEAD `416dcbb`, working tree limpio.

## Objetivo del bloque
Cerrar el smoke E2E que en el chat web no se pudo validar por bugs ambientales
(Streamlit con python 3.14 global, cwd confuso, "save silenciosamente fallido").

## Diagnóstico del save (OBJETIVO 3) — NO era bug de código

- `save_proposal` (`core/proposal_persistence.py:318-346`) valida → escribe → retorna,
  **sin try/except** en toda la capa de persistencia. Si retorna un dict, `write_proposal`
  se ejecutó. La hipótesis "validation error que retorna dict sin escribir" queda
  **descartada** (un validation error lanza `ValueError`, no retorna).
- El único try/except del flujo está en la UI (`proposal_studio.py:2871`) y **no silencia**:
  muestra `st.error` con tipo+mensaje real de la excepción.
- **Causa raíz de fondo:** `proposal_paths.py:19` → `SALES_ROOT = Path("data")/"sales"` es
  **relativo al cwd** del proceso Streamlit, no a `__file__`. `app.py` no hace `os.chdir`.
  Lectura y escritura usan el mismo root relativo → consistentes entre sí, pero el archivo
  aterriza en el árbol `data/sales/proposals` de donde sea el cwd. En el web, el v13 (si se
  escribió) fue a otro árbol del que miraron. **Fix `Path(__file__)...` = deuda P1 fuera de
  scope M29, NO aplicado (afecta a todo el sistema Sales).**
- **Caveat UI menor:** el banner verde de éxito (`:2897`) se pinta y enseguida
  `st.rerun()` (`:2912`) lo descarta → puede dar la sensación de "no pasó nada" aunque el
  v13 sí se escribió. Deuda registrada, no tocado.

## Smoke determinístico (OBJETIVO 5 + 7) — GREEN

Hecho vía CLI con el venv 3.12.10 del principal (`C:\proyectos\ppc-manager\.venv`),
cwd = worktree, llamando exactamente las funciones de la UI
(`get_proposal → extract_blocks → merge_blocks → save_proposal`).

| Caso | Resultado |
|---|---|
| Happy path (`b7_sample_v3v4.html`) | report.ok, blocks=2, warnings=6, errors=0; merge aplica V3+V4; **v12 → v13 en disco** (13164 bytes); data V3/V4 cambió vs v12 |
| Duplicate (`b7_sample_duplicate.html`) | warning `duplicate_module_id_in_html` presente (fix D6); merge aplica último de los duplicados; **v13 → v14 en disco** |
| Inválido (`b7_sample_invalid.html`, nuevo) | report.ok=False, errors=`['no_blocks']`, 0 blocks → no hay apply |

`proposal_studio.py` (con edits D3 + popover) compila (`py_compile OK`) e importa limpio.

## Suite de tests (OBJETIVO 1+2 del bloque consolidador) — 76/76 PASS
- pytest 9.0.3 instalado en el venv 3.12 (la suite se corría histórico bajo 3.14).
- `pytest tests/` → **76 passed, 0 fail/error/skip** bajo Python 3.12.10.
  - test_b7_importer.py: 10 · test_proposal_persistence.py: 41 · test_proposal_schema.py: 22 · test_inicio_badge.py: 3
- 0 regresiones de D3/D4 → merge a main autorizado y ejecutado.

## Setup de data en el worktree (OBJETIVO 4)
- `data/sales/` ya tenía `_catalog.json` + `_templates/` + `_seed/` (todo tracked en git).
- Único faltante: el proposal gitignored → copiado `01fbf5c2...__v12.json` del principal.
- v13/v14 generados por el smoke quedan en `data/sales/proposals/` (gitignored, untracked OK).

## Caveats worktree + data dirs (para SOP worktrees-flow)
1. **`data/sales/proposals/` arranca vacío** (solo `.gitkeep`, proposals gitignored). Todo
   smoke con proposals reales requiere copiar el/los `__vN.json` del principal primero.
2. **`.streamlit/secrets.toml` no existe en el worktree** → `app.py:152` lanza
   `FileNotFoundError: No secrets found` y la app no renderiza la página (gate de login).
   Para smoke de UI completo hay que copiar `secrets.toml` del principal o de
   `~/.streamlit/`. El smoke CLI lo esquiva (no usa Streamlit runtime).
3. **El venv 3.12 ya tiene pytest** (instalado este día, `pytest==9.0.3`). La suite corre
   verde bajo 3.12.
4. **`python -m streamlit run` con el venv spawnea el worker bajo otro intérprete**
   (Python312 de `Programs`, child del launcher venv). Ambos son 3.12 acá, pero conviene
   saberlo: el server real no corre necesariamente bajo `sys.executable` del launcher.
5. **Race del principal entre frentes:** durante este bloque, mientras esperaba decisión,
   otra sesión commiteó+pusheó `b2763cb fix(M4)` (los 313 ins de analisis_cruzado.py que
   estaban sin commitear) y main se movió `5fa3e02 → b2763cb`. El merge se rebasó contra el
   nuevo HEAD sin drama. Hay un stash ajeno (`stash@{0}` del frente M27) — NO tocado.

## Procesos
- Matados los 2 fantasmas conocidos (PID 51912 WindowsApps + 8908 python 3.14).
- Streamlit de prueba lanzado y matado; entorno cerrado limpio (0 procesos python).

## Estado D3+D4
- D3 (apply 2-clicks + merge + save auto-bump): **validado E2E happy path**.
- D4 (edges duplicate + inválido): **validado**.
- Suite 76/76 PASS bajo 3.12 → **mergeado a main** (merge --no-ff + push).
- Pendiente real: smoke de UI manual en browser (bloqueado por secrets.toml + requiere
  clicks humanos) — la lógica está cubierta por el smoke CLI determinístico + la suite.

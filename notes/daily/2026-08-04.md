---
tipo: daily
fecha: 2026-08-04
---
# Daily 2026-08-04 — Consolidador: M31 fc-multi a producción

## Consolidación
Frente `feature/m31-fc-multi` integrado a main por **fast-forward** (`cca5175..83f89e1`) y pusheado. 3 commits, 3 archivos (973 inserciones), scope 100% M31:
- `723051d` — fix(m31): rerun tras cargar mes real (guard de firma nombre+tamaño, NO st.rerun desnudo) + limpiar actual aislado + quitar UI de margen.
- `d6bc893` — feat(m31): snapshots de forecast nombrados (guardar/cargar/borrar/comparar), persistidos vía `_try_persist()` sobre `cur["snapshots"]`. CERO DDL, cero tabla nueva. Aislamiento por `copy.deepcopy`.
- `83f89e1` — fix(m31): importer By ASIN — inferencia de período cubre naming Amazon (`BusinessReport-M-DD-YY`) además de ISO + real-vs-forecast por ASIN.

La branch resultó ser ancestro-limpio de `origin/main` (FF puro, sin rebase necesario): ya contenía los 16 commits de ordenamiento del vault. Suite de los 2 tests nuevos: **42 passed** local. Resumen del frente reporta M31 completa 442 passed / 24 skipped.

## Prod
Sin acción de setup. El flag `forecast_backend="supabase"` YA estaba en Secrets de Streamlit Cloud (la deuda del 2/7-jul estaba desactualizada). Persistencia de snapshots operativa apenas redespliega.

## Hallazgos
1. **El bug A1 no era stale-chart.** `cur["actual"]` es dict vivo de session_state; los charts ya lo leían fresco. El `st.rerun()` desnudo propuesto HABRÍA LOOPEADO INFINITO (uploader retiene archivo → re-parsea → rerun → …). Resuelto con guard de firma nombre+tamaño.
2. **BUG PENDIENTE (registrado en STATE ítem 20, decisión abierta):** re-subir el MISMO mes corregido NO actualiza Spend ni Ventas PPC — `_merge_historical` los preserva por diseño (inputs manuales del AM). Si el AM corrige esos 2 campos en el CSV, el merge los ignora.
3. **Restricción dura importer By ASIN (reconfirmada):** "Detail Page Sales and Traffic By Child Item" NO trae spend ni ventasPPC. El real-vs-forecast por ASIN solo grafica revenue/sessions/units/CVR. ACOS/TACOS/Spend por ASIN NO existen; pedirlos lanza ValueError (test pinea `_ASIN_CHART_METRICS`).
4. **Falsa alarma de encoding en STATE-agencia.md:** se sospechaba mojibake preexistente (`Ã³`, `â€"`, `Ã±`). Verificado byte-level: el archivo es **UTF-8 limpio** — `quedó` es `c3b3`, `file` reporta "UTF-8 text". Lo que se veía como mojibake era el artefacto de un visor decodificando UTF-8 como Latin-1, no el contenido en disco. NO hay renormalize pendiente sobre este archivo. Lección: verificar con `xxd`/`file` antes de asumir corrupción de encoding — la doble-decodificación en un viewer es indistinguible a simple vista de la corrupción real.

## Fixture
`tests/fixtures/real/BusinessReport-8-04-26.csv` gitignored, NO commiteado (dato de cliente). Confirmado en vivo: → 2026-08, 9 ASINs parseados.

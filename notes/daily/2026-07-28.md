---
tipo: daily
actualizado: 2026-07-28
---

# 2026-07-28

## Frente consolidador - M36 Mercado Libre a produccion

Modulo nuevo mergeado a main (`ebcd0cb`, --no-ff) y pusheado. 23 archivos, 2637 inserciones, cero borrados. Tres features: Listing Change Tracker, Sugerencia de stock, Alertas de Ads. Multi-cuenta, carga manual de Excel, sin API. Pedido por Cleimery Bravo y Josefina Mastromatteo.

### Verificacion
- Gate de borrados de vault vacio con y sin `--ignore-cr-at-eol`, y `--diff-filter=D` vacio en todo el diff. La branch venia de un main anterior, pero su merge de `57b68fe` la habia puesto al dia.
- Byte-level en las 2 notas del vault y 2 modulos: sin mojibake, sin BOM, CRLF 0.
- Suite 742 passed / 5 skipped / 9 failed conocidos. Ninguno en `modules/mercado_libre/`.

### 🔴 Nota del vault corrupta, cazada por el gate
`notes/modules/m36-mercado-libre.md` tenia 2 bytes `0xB3` huerfanos: la palabra "Modulo" habia perdido el lead byte `0xC3` de la o acentuada, en L010 y L014. Reparado a nivel bytes (4352 -> 4354) y verificado antes de commitear. **Sin el gate byte-level se commiteaba corrupta.** Mismo modo de falla que documenta la seccion de `Get-Content -Raw` en CLAUDE.md.

### Las 2 notas del vault las commiteo el frente
El handoff pedia que las incluyera el consolidador, pero solo existian en el worktree del frente. Se commitearon en su branch (`f861fd1` SOP de usuario, `9370763` nota de modulo): un solo push, un solo redeploy.

Detalle en [[m36-mercado-libre]]

## Frente consolidador - `_PAGES` incompleto (bug user-visible)

Salio de tirar del hilo de un pendiente que parecia cosmetico.

`_PAGES` en `core/constants.py` no es lista muerta: `modules/pages/inicio.py:15` deriva `_TOTAL_MODULOS` de ella para el badge de modulos activos del home. `app.py:8` la importa pero **no la usa** (import muerto de cuando la nav era un `st.radio`; hoy son botones por expander).

Le faltaban dos entradas: Mercado Libre y **Case Study Studio, ausente desde M32**. El badge contaba 30 sobre 32 de la nav. Corregido en `2c34d1f`: `_PAGES` quedo en 33 = Inicio + 32.

- Los 3 tests previos de `test_inicio_badge.py` **no detectan faltantes**: dos comparan `_TOTAL_MODULOS` contra `_PAGES` y pasan con cualquier contenido, el tercero es un pin a mano de Listing Monitor. Por eso Case Study Studio quedo afuera meses sin que la suite chille.
- Se sumaron 2 pins del mismo tipo. **No es la solucion estructural:** falta un test que compare los labels de los `st.button` de `app.py` contra `_PAGES`.

## Frente consolidador - M31 F7 Fase A (Real vs Forecast)

Mergeado (`b4d080e`, --no-ff) y pusheado. 7 commits del frente `feature/m31-real-vs-forecast`, 5 archivos, 1656 inserciones. Capa `actual`: linea verde de real contra forecast en los 7 charts, uploader de mes en curso, marcador de mes parcial y export HTML con la linea real. Pedido de Eduardo en la reunion del 27-jul. Cierra el ciclo forecast -> real -> decision a nivel cuenta.

### 🔴 Esta en produccion SIN PERSISTIR
Sigue faltando `AGENCY_OS_FORECAST_BACKEND="supabase"` en los Secrets de Streamlit Cloud, deuda abierta desde el 02-jul. En local anda; en `capybaras-os.streamlit.app` no. **Si Edu carga un mes real en prod, lo pierde al cerrar la pestana. No avisarle hasta setear el flag.**

### Verificacion
- Gate de borrados vacio. El frente no toca `notes/` en absoluto.
- Cero superposicion con lo pusheado antes en el dia (`constants.py`, `test_inicio_badge.py`, `CLAUDE.md`, `app.py`, `modules/mercado_libre/`).
- Suite 823 passed / 5 skipped / 9 failed sobre 837 colectados. Los 9 son los mismos nombres de antes del merge: la familia forecast no se movio pese a 633 lineas reescritas en `revenue_forecast.py`.
- Falsa alarma descartada: `_read_br_rows` es una extraccion **aditiva**. `_parse_business_report` conserva su `@st.cache_data` y su contrato, con guard de no-regresion propio.

## Deudas y estado

- ✅ **La regla del 22-jul se valido por segunda vez.** El gate de borrados con y sin `--ignore-cr-at-eol` se corrio en los dos merges del dia y dio vacio en ambos. Sigue siendo la unica forma de detectar borrados silenciosos: `merge-tree` sale limpio igual.
- ⚠️ **Renormalizacion EOL: el repo quedo en estado mixto.** Se ejecuto una rebanada (`b92c71f`, 2 archivos) aislada en su propio commit, porque 156 lineas de ruido tapaban un cambio de 12. Pero la tarea sigue scopeada como repo-wide y del consolidador: faltan los ~39 archivos del 15-jul, los 27 del worktree `-realvsfc` y los de `-charts` e `-innovation`.
- ⚠️ **Worktree `ppc-manager-realvsfc`:** 27 archivos modificados solo por EOL, sin commitear, entre ellos `tests/test_inicio_badge.py` en version previa a los cambios de hoy, y 11 commits atras de main. Si el frente commitea ahi sin actualizar, pisa el trabajo del dia. Segun la decision del 21-jul **el frente no debe tocar ni limpiar esos archivos**: los resuelve el consolidador.
- M36 entro **sin tests propios del modulo**; la validacion E2E del frente fue manual. Tercer frente seguido sin cobertura, despues de Campaign Analyzer (20-jul).
- Los 9 rojos por corrida son la deuda de aislamiento de tests ya priorizada el 22-jul, no ruido nuevo.
- Falta la fila de M29 Proposal Studio en la tabla de navegacion de CLAUDE.md, y sacar el import muerto de `_PAGES` en `app.py:8`.

## 🟠 Pendiente de verificar

Ninguno de los dos modulos se verifico en el deploy. Falta confirmar en `capybaras-os.streamlit.app` que aparece la seccion MARKETPLACES y que un dato cargado sobrevive a un refresh, y lo mismo para la linea verde de M31.
